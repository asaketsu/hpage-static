<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>mise-开发环境管理工具</title>

<meta property="og:title" content="mise-开发环境管理工具" />
<meta property="og:type" content="article" />
<meta property="og:description" content="笔记 | security" />
<meta property="og:image" content="https://hpage.top/homepage/covers/2026-08-07-mise-%E5%BC%80%E5%8F%91%E7%8E%AF%E5%A2%83%E7%AE%A1%E7%90%86%E5%B7%A5%E5%85%B7.png" />

<style>
@import url('https://fonts.googleapis.com/css2?family=Noto+Sans+SC:wght@400..700&family=Noto+Serif+SC:wght@400..700&family=Space+Grotesk:wght@400..700&family=JetBrains+Mono:wght@400..500&family=IBM+Plex+Sans:wght@400..600&display=swap');

* { box-sizing: border-box; }

body {
  font-family: 'Fira Code', 'JetBrains Mono', monospace;
  max-width: 840px;
  margin: 0 auto;
  padding: 32px 24px;
  line-height: 1.85;
  font-weight: 450;
  color: #1f2937;
  background: #f3f4f6;
}

h1 {
  font-family: 'JetBrains Mono', 'Space Mono', monospace;
  font-size: 1.75em;
  font-weight: 700;
  color: #111827;
  border-bottom: none;
  padding-bottom: 0;
  margin-top: 0;
  margin-bottom: 0.6em;
}

h2 {
  font-family: 'JetBrains Mono', 'Space Mono', monospace;
  font-size: 1.35em;
  font-weight: 600;
  color: #111827;
  margin-top: 1.8em;
  margin-bottom: 0.5em;
  border-bottom: 1px solid #e5e7eb;
  padding-bottom: 6px;
}

h3 {
  font-family: 'JetBrains Mono', 'Space Mono', monospace;
  font-size: 1.12em;
  font-weight: 600;
  color: #111827cc;
  margin-top: 1.4em;
}

h4 {
  font-size: 1em;
  font-weight: 600;
  color: #111827aa;
}

a {
  color: #dc2626;
  text-decoration: none;
  border-bottom: 1px solid #dc262630;
  transition: border-color 0.2s;
}

a:hover {
  border-bottom-color: #dc2626;
}

code {
  background: #fef2f2;
  color: #dc2626;
  padding: 2px 7px;
  border-radius: 4px;
  font-size: 0.88em;
  font-family: 'JetBrains Mono', 'SF Mono', 'Fira Code', monospace;
}

pre {
  background: #111827;
  color: #f9fafb;
  border: none;
  padding: 20px 24px;
  border-radius: 8px;
  overflow-x: auto;
  line-height: 1.6;
  font-size: 0.88em;
  margin: 1.2em 0;
  box-shadow: 0 2px 8px rgba(0,0,0,0.08);
}

pre code {
  background: none;
  color: inherit;
  padding: 0;
  font-size: inherit;
}

blockquote {
  border-left: 4px solid #dc2626;
  margin: 1.2em 0;
  padding: 12px 20px;
  color: #1f2937cc;
  background: #fef2f2;
  border-radius: 0 6px 6px 0;
}

/* ═══ table：彻底禁用 ═══ */
table, th, td { display: none !important; }

img {
  max-width: 100%;
  border-radius: 8px;
  margin: 8px 0;
  box-shadow: 0 1px 4px rgba(0,0,0,0.06);
}

hr {
  border: none;
  height: 1px;
  background: #e5e7eb;
  margin: 2.5em 0;
}

ul, ol {
  padding-left: 1.6em;
}

li { margin-bottom: 4px; }

strong { color: #111827; }


        h1::before { content: '🛡 '; }
        pre { border-left: 3px solid #dc2626; }
        

/* 响应式 */
@media (max-width: 640px) {
  body { padding: 20px 16px; }
  h1 { font-size: 1.45em; }
  h2 { font-size: 1.2em; }
  pre { padding: 14px 16px; font-size: 0.82em; }
}

/* 打印优化 */
@media print {
  body { background: white; color: black; max-width: 100%; }
  pre { border: 1px solid #ddd; box-shadow: none; }
  a { color: #333; border-bottom: none; }
}
</style>
</head>
<body>
<h1 id="mise-mise-en-place-cli">mise (mise-en-place) — 开发工具 + 环境变量 + 任务管理一体化 CLI</h1>
<blockquote>
<p><strong>来源</strong>：<a href="https://github.com/jdx/mise">github.com/jdx/mise</a><br />
<strong>文档</strong>：<a href="https://mise.jdx.dev">mise.jdx.dev</a><br />
<strong>License</strong>：MIT<br />
<strong>Stars</strong>：31.9K · <strong>Forks</strong>：1.3K<br />
<strong>语言</strong>：Rust<br />
<strong>作者</strong>：Jeff Dickey (jdx)<br />
<strong>赞助商</strong>：entire.io, 37signals, Namespace</p>
</blockquote>
<hr />
<h2 id="_1">一句话定义</h2>
<blockquote>
<p>mise（发音 /meez ahn plahs/，源自法语"准备就绪"）— 用一个 <code>mise.toml</code> 文件管理项目的<strong>开发工具版本 + 环境变量 + 任务</strong>，让每台机器获得相同的开发环境。</p>
</blockquote>
<hr />
<h2 id="_2">内容整理</h2>
<h3 id="everything-in-its-place-before-you-code">核心理念：Everything in its place, <em>before</em> you code</h3>
<blockquote>
<p>mise en place（法语）：烹饪前收集和整理所有食材和工具的过程。</p>
</blockquote>
<p>mise 把这个理念用在开发环境上——在写代码之前，把工具、环境变量、任务都准备好。</p>
<h3 id="_3">三大功能</h3>
<h4 id="1-dev-tools">1. Dev Tools — 开发工具版本管理</h4>
<table>
<thead>
<tr>
<th>能力</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td>版本安装</td>
<td>1000+ 工具，一个配置文件</td>
</tr>
<tr>
<td>自动切换</td>
<td>进入项目目录自动切换到指定版本</td>
</tr>
<tr>
<td>版本锁定</td>
<td>在 <code>mise.toml</code> 中 pin 版本</td>
</tr>
<tr>
<td>跨平台</td>
<td>Linux / macOS / Windows / WSL</td>
</tr>
</tbody>
</table>
<p>支持的工具包括：node, python, ruby, go, rust, java, deno, bun, terraform, kubectl, zig, swift, php, elixir 等 <strong>1000+</strong>。</p>
<h4 id="2-environments">2. Environments — 环境变量管理</h4>
<ul>
<li>从 <code>mise.toml</code> 加载项目特定环境变量</li>
<li>支持 <code>.env</code> 文件</li>
<li>支持 shell 命令动态生成变量</li>
<li>进入目录自动激活</li>
</ul>
<h4 id="3-tasks">3. Tasks — 任务管理</h4>
<ul>
<li>定义 build / test / lint / deploy 命令</li>
<li>任务与所需的工具和环境变量绑定在一起</li>
<li>用 <code>mise run &lt;task&gt;</code> 执行</li>
</ul>
<h3 id="misetoml">mise.toml 配置示例</h3>
<pre><code class="language-toml">[tools]
node = &quot;24&quot;
python = &quot;3.13&quot;

[env]
_.file = &quot;.env.local&quot;

[tasks.test]
run = &quot;pytest&quot;
</code></pre>
<h3 id="_4">四步快速上手</h3>
<pre><code class="language-bash"># 1. 安装 mise
curl https://mise.run | sh

# 2. 验证版本
mise --version
# 2026.7.0 linux-x64

# 3. 安装工具
mise use node@24 python@3.13

# 4. 使用
node --version          # v24.18.0
mise env -s bash        # 输出环境变量
mise run test           # 执行任务
</code></pre>
<h3 id="mise-vs">mise vs 竞品</h3>
<table>
<thead>
<tr>
<th>特性</th>
<th>mise</th>
<th>nvm/pyenv/asdf</th>
<th>direnv</th>
<th>make/just</th>
</tr>
</thead>
<tbody>
<tr>
<td>工具版本管理</td>
<td>✅ 1000+</td>
<td>✅（每个语言一个工具）</td>
<td>❌</td>
<td>❌</td>
</tr>
<tr>
<td>环境变量</td>
<td>✅</td>
<td>❌</td>
<td>✅</td>
<td>❌</td>
</tr>
<tr>
<td>任务运行器</td>
<td>✅</td>
<td>❌</td>
<td>❌</td>
<td>✅</td>
</tr>
<tr>
<td>单一配置文件</td>
<td>✅ <code>mise.toml</code></td>
<td>分散</td>
<td><code>.envrc</code></td>
<td><code>Makefile</code>/<code>justfile</code></td>
</tr>
<tr>
<td>语言</td>
<td>Rust（快）</td>
<td>Bash/Python</td>
<td>Go</td>
<td>C/Go</td>
</tr>
<tr>
<td>速度</td>
<td>快（Rust）</td>
<td>慢（Shell 脚本）</td>
<td>中</td>
<td>中</td>
</tr>
</tbody>
</table>
<p>mise 的核心优势：<strong>三合一</strong>——把 nvm/pyenv/asdf（版本管理）+ direnv（环境变量）+ make/just（任务）合并为一个工具，一个配置文件。</p>
<h3 id="aube">作者其他项目：aube</h3>
<p>作者还开发了 <strong>aube</strong> — 一个快速的 Node.js 包管理器，与现有 lockfile 兼容，无需迁移。</p>
<hr />
<h2 id="_5">查询拓展</h2>
<ul>
<li>mise 的工具安装机制——是自己编译还是下载预编译二进制？</li>
<li><code>mise.toml</code> 的 <code>[env]</code> 支持动态变量（shell 命令输出）吗？</li>
<li>mise 的任务系统与 just/Make 有什么区别？是否支持依赖链？</li>
<li>在 CI/CD（GitHub Actions / GitLab CI）中如何使用 mise？</li>
<li>mise 的 registry 如何添加自定义工具？</li>
<li>mise vs asdf 的迁移路径？asdf 插件是否兼容？</li>
</ul>
<hr />
<h2 id="_6">归纳总结</h2>
<p>mise 是开发环境管理领域的"瑞士军刀"——用 Rust 编写、31.9K Stars，把三个最常见的需求（版本管理、环境变量、任务）合并到一个 CLI 和一个 <code>mise.toml</code> 文件中。</p>
<p>它的价值主张很简洁：<strong>把 mise.toml 提交到仓库，任何人 clone 后运行 <code>mise install</code> 就获得完全一致的开发环境</strong>。不需要 README 里写"请先安装 nvm，然后 pyenv，然后…，然后记得 direnv allow"。</p>
<p>对多语言项目（Node + Python + Go 混合）特别有价值——以前需要 nvm + pyenv + goenv 三个工具，现在一个 mise 全搞定。</p>
<hr />
<h2 id="_7">趋势判断</h2>
<ul>
<li><strong>开发环境即代码（Dev Environment as Code）</strong> 成为标配——mise.toml / .tool-versions / flake.nix / devcontainer.json 各路线竞争，mise 的 TOML 最简洁</li>
<li><strong>Rust 重写 CLI 工具浪潮</strong>：mise(Rust) 替代 asdf(Bash)、ripgrep(Rust) 替代 grep、fd(Rust) 替代 find、bat(Rust) 替代 cat——Rust 的零启动开销让 CLI 工具速度跨代提升</li>
<li><strong>多语言项目增多</strong>：全栈项目（Node + Python + Go + Rust）的普及让单一版本管理工具的需求更迫切</li>
<li><strong>CI/CD 标准化</strong>：mise 在 CI 中使用可以保证本地和 CI 环境一致，减少"在我机器上能跑"的问题</li>
</ul>
<hr />
<h2 id="_8">关联分析</h2>
<ul>
<li><strong>与 Hermes Agent 的关联</strong>：Hermes 运行在 Jetson Orin Nano 上，用 Python venv 管理依赖。mise 可以管理 Python 版本本身（<code>mise use python@3.12</code>），替代 pyenv，让 Hermes 的 Python 环境版本管理更可靠</li>
<li><strong>与用户多语言开发的关联</strong>：用户同时使用 Python（Hermes/LuxTTS）、Rust（dynibo）、Node.js（homepage/md2html）、C++（QtScrcpy/嵌入式），mise 是天然的多语言版本管理工具</li>
<li><strong>与 python-env-isolation Skill 的关联</strong>：用户有 <code>python-env-isolation</code>（venv 隔离与依赖安装防污染）Skill，mise 可以管理 Python 解释器版本本身，与 venv 形成互补——mise 管 Python 版本，venv 管包</li>
<li><strong>与 Jetson 平台的关联</strong>：Jetson 是 ARM64，部分工具需要特殊编译。mise 可以简化 Jetson 上的多版本工具管理</li>
</ul>
<hr />
<h2 id="_9">📎 关键链接</h2>
<ul>
<li>📖 官方文档：<a href="https://mise.jdx.dev">mise.jdx.dev</a></li>
<li>💻 GitHub：<a href="https://github.com/jdx/mise">github.com/jdx/mise</a></li>
<li>📦 安装：<code>curl https://mise.run | sh</code></li>
<li>📋 工具注册表：<a href="https://mise-versions.jdx.dev">mise-versions.jdx.dev</a></li>
<li>💬 Discord：<a href="https://discord.gg/mABnUDvP57">discord.gg/mABnUDvP57</a></li>
</ul>
<hr />
<p><em>笔记创建：2026-08-07 · 来源：github.com/jdx/mise</em></p>
<h2 id="_10">相关笔记</h2>
<ul>
<li><a href="2026-08-04-dlt-开源Python数据加载库.md">dlt-开源Python数据加载库</a></li>
<li><a href="2026-08-07-OpenDataLoader-PDF-AI就绪PDF解析器.md">OpenDataLoader-PDF-AI就绪PDF解析器</a></li>
</ul>
<hr />
</body>
</html>