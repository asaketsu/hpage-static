<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>虚拟小智浏览器模拟ESP32设备</title>

<meta property="og:title" content="虚拟小智浏览器模拟ESP32设备" />
<meta property="og:type" content="article" />
<meta property="og:description" content="笔记 | academic" />
<meta property="og:image" content="https://hpage.top/homepage/covers/academic.png" />

<style>
@import url('https://fonts.googleapis.com/css2?family=Noto+Sans+SC:wght@400..700&family=Noto+Serif+SC:wght@400..700&family=Space+Grotesk:wght@400..700&family=JetBrains+Mono:wght@400..500&family=IBM+Plex+Sans:wght@400..600&display=swap');

* { box-sizing: border-box; }

body {
  font-family: 'Noto Serif SC', 'Source Serif 4', Georgia, serif;
  max-width: 840px;
  margin: 0 auto;
  padding: 32px 24px;
  line-height: 1.85;
  font-weight: 450;
  color: #2c2c2c;
  background: #faf8f5;
}

h1 {
  font-family: 'Noto Serif SC', 'Playfair Display', Georgia, serif;
  font-size: 1.75em;
  font-weight: 700;
  color: #8b1a1a;
  border-bottom: none;
  padding-bottom: 0;
  margin-top: 0;
  margin-bottom: 0.6em;
}

h2 {
  font-family: 'Noto Serif SC', 'Playfair Display', Georgia, serif;
  font-size: 1.35em;
  font-weight: 600;
  color: #8b1a1a;
  margin-top: 1.8em;
  margin-bottom: 0.5em;
  border-bottom: 1px solid #e8e0d4;
  padding-bottom: 6px;
}

h3 {
  font-family: 'Noto Serif SC', 'Playfair Display', Georgia, serif;
  font-size: 1.12em;
  font-weight: 600;
  color: #8b1a1acc;
  margin-top: 1.4em;
}

h4 {
  font-size: 1em;
  font-weight: 600;
  color: #8b1a1aaa;
}

a {
  color: #8b4513;
  text-decoration: none;
  border-bottom: 1px solid #8b451330;
  transition: border-color 0.2s;
}

a:hover {
  border-bottom-color: #8b4513;
}

code {
  background: #f7f5f0;
  color: #9a5b13;
  padding: 2px 7px;
  border-radius: 4px;
  font-size: 0.88em;
  font-family: 'JetBrains Mono', 'SF Mono', 'Fira Code', monospace;
}

pre {
  background: #3c3226;
  color: #f5f0e8;
  border: none;
  padding: 20px 24px;
  border-radius: 8px;
  overflow-x: auto;
  line-height: 1.6;
  font-size: 0.88em;
  margin: 1.2em 0;
  box-shadow: 0 2px 8px rgba(0,0,0,0.08);
}

pre code {
  background: none;
  color: inherit;
  padding: 0;
  font-size: inherit;
}

blockquote {
  border-left: 4px solid #a0522d;
  margin: 1.2em 0;
  padding: 12px 20px;
  color: #2c2c2ccc;
  background: #f5f0e8;
  border-radius: 0 6px 6px 0;
}

/* ═══ table：彻底禁用 ═══ */
table, th, td { display: none !important; }

img {
  max-width: 100%;
  border-radius: 8px;
  margin: 8px 0;
  box-shadow: 0 1px 4px rgba(0,0,0,0.06);
}

hr {
  border: none;
  height: 1px;
  background: #e8e0d4;
  margin: 2.5em 0;
}

ul, ol {
  padding-left: 1.6em;
}

li { margin-bottom: 4px; }

strong { color: #8b1a1a; }


        h1 { font-style: italic; }
        h1::before { content: '§ '; color: #a0522d40; font-size: 0.7em; vertical-align: middle; }
        

/* 响应式 */
@media (max-width: 640px) {
  body { padding: 20px 16px; }
  h1 { font-size: 1.45em; }
  h2 { font-size: 1.2em; }
  pre { padding: 14px 16px; font-size: 0.82em; }
}

/* 打印优化 */
@media print {
  body { background: white; color: black; max-width: 100%; }
  pre { border: 1px solid #ddd; box-shadow: none; }
  a { color: #333; border-bottom: none; }
}
</style>
</head>
<body>
<h1 id="iot">从零打造虚拟小智：用浏览器模拟 IoT 设备的实践之路</h1>
<blockquote>
<p><strong>来源</strong>：webmote · 微信公众平台 · dotnet微服务<br />
<strong>原文</strong>：https://mp.weixin.qq.com/s/HiXcEmQcl8Enfi-Fb-cE0g<br />
<strong>笔记ID</strong>：45d97b325372<br />
<strong>格式</strong>：双格式(MD + HTML) · 含借鉴进化点 · RAG/wiki联动</p>
</blockquote>
<h2 id="_1">摘要</h2>
<p>本文介绍了虚拟小智模拟器（SimDevice）的开发过程，旨在通过浏览器模拟基于ESP32的AI语音对话设备。开发背景源于硬件调试效率瓶颈，作者提出了基于 WebSocket/MQTT 的浏览器模拟方案。核心架构为三层：浏览器前端（Web Codecs + ScriptProcessor）→ ASP.NET Core 桥接层（SimDeviceBridge）→ 小智 AI 服务器。涉及 OTA 协议分析、Opus 音频编解码、21种情感 GIF 系统、唤醒词打断等完整功能链。整个项目约两周业余时间完成。</p>
<h2 id="_2">核心内容</h2>
<h3 id="1">1. 为什么要造这个轮子</h3>
<p>小智（XiaoZhi）是一款基于 ESP32 的开源 AI 语音对话设备，通过 WebSocket/MQTT 与后端实时通信。调试痛点：</p>
<ul>
<li>每次测试都需要真实硬件，场景受限</li>
<li>想测试 AI 服务是否正常，不想等设备重启</li>
<li>需要批量回归测试 TTS 音色或唤醒词</li>
<li>给非硬件团队演示 AI 对话效果</li>
</ul>
<p><strong>理想状态</strong>：打开网页 → 点击连接 → 像真实设备一样与 AI 对话。</p>
<p>完成后发现这不仅是调试工具，本身就是一个产品功能：<strong>没买硬件的用户也能体验 AI 对话</strong>。</p>
<h3 id="2">2. 三层架构设计</h3>
<p>为什么不能浏览器直连小智服务器？两个硬限制：</p>
<ol>
<li><strong>WebSocket 不支持自定义 Header</strong>：小智服务器要求 Device-Id、Client-Id、Authorization 等自定义 Header，浏览器 WebSocket API 不允许设置</li>
<li><strong>CORS 跨域限制</strong>：OTA 接入流程需要先 HTTP POST 获取 WebSocket 地址和 Token，浏览器端全部被 CORS 拦截</li>
</ol>
<p>解决方案——中间桥接层：</p>
<table>
<thead>
<tr>
<th>组件</th>
<th>技术</th>
<th>职责</th>
</tr>
</thead>
<tbody>
<tr>
<td>SimDeviceBridge</td>
<td>C# ASP.NET Core</td>
<td>代替浏览器执行 OTA HTTP 请求，携带自定义 Header，建立 ClientWebSocket 连接，双向转发消息</td>
</tr>
<tr>
<td>ScriptProcessor</td>
<td>浏览器 getUserMedia</td>
<td>麦克风 PCM 采集 → Int16 转换 → 上传 Bridge</td>
</tr>
<tr>
<td>Web Codecs API</td>
<td>Chromium AudioDecoder</td>
<td>Opus 帧硬件加速解码 → 实时播放</td>
</tr>
<tr>
<td>情感 GIF</td>
<td>noto-emoji 21种</td>
<td>服务器 emotion 字段驱动切换</td>
</tr>
</tbody>
</table>
<h3 id="3-ota">3. OTA 协议与激活流程</h3>
<p>小智设备上电后请求 OTA 接口（HTTP POST），Body 是设备硬件信息 JSON，Response 返回 WebSocket 地址和 Token。</p>
<p><strong>激活流程</strong>：<br />
1. 未绑定设备 → OTA 返回 activation 对象（含6位激活码）<br />
2. 设备展示激活码，用户在 App 输入<br />
3. 设备轮询 /activate 端点，直到激活成功</p>
<h3 id="4-opus">4. 音频管道：Opus 编解码</h3>
<p>小智协议使用 <strong>Opus 编码，16kHz 单声道，60ms/帧</strong>。</p>
<p><strong>上行（麦克风 → 服务器）</strong>：<br />
- getUserMedia 采集 PCM → ScriptProcessor（2048 samples）→ Float32 转 Int16 → 发送 Bridge<br />
- Bridge 用 Concentus（C# Opus 编码器）编码 → WebSocket 发给小智服务器</p>
<p><strong>下行（服务器 → 扬声器）</strong>：<br />
- 早期用 Concentus.js（WebAssembly）解码，延迟高、内存大<br />
- 改用 Chromium 原生 <strong>Web Codecs API（AudioDecoder）</strong>，硬件加速解码，延迟降一个数量级</p>
<h3 id="5">5. 实时模式与回声消除</h3>
<p>普通模式：TTS 播放时暂停麦克风上传（防回声）。<br />
实时模式：允许用户随时打断 AI → 同时上报麦克风 + 播放 TTS → 服务端 AEC 剔除扬声器输出。</p>
<h3 id="6">6. 情感系统</h3>
<ul>
<li>服务器 llm 消息含 emotion 字段（如 "thinking"）</li>
<li>从 noto-emoji 提取 21 种情感 128px GIF 动图</li>
<li>TTS 说话期间保持当前情感（不切换到扬声器图标），说话结束恢复最近 LLM 情感</li>
<li><strong>细节决定灵魂</strong>：AI 说话时脸上表情依然是"高兴"或"思考"</li>
</ul>
<h3 id="7">7. 唤醒词打断</h3>
<p>浏览器用 <strong>Web Speech Recognition API</strong>（webkitSpeechRecognition）做实时唤醒词检测，监听到"你好小智"后立即发送 abort + 停止 TTS。</p>
<h3 id="8">8. 踩过的坑</h3>
<table>
<thead>
<tr>
<th>坑</th>
<th>描述</th>
</tr>
</thead>
<tbody>
<tr>
<td>ScriptProcessor 即将废弃</td>
<td>官方推荐 AudioWorklet，但兼容性尚未成熟</td>
</tr>
<tr>
<td>Web Codecs timestamp 严格递增</td>
<td>AudioDecoder.decode() 要求 timestamp 严格递增（微秒），关闭重建必须归零否则 Chrome 抛 EncodingError</td>
</tr>
<tr>
<td>WebSocket 自定义 Header 无解</td>
<td>浏览器硬限制，只能靠中间代理</td>
</tr>
<tr>
<td>GIF 重播</td>
<td>同一 emotion 不重新加载 GIF 会停止动画，需克隆节点重播</td>
</tr>
<tr>
<td>双重 disconnected</td>
<td>Close 帧处理 + finally 块各发一次，导致浏览器两次状态重置</td>
</tr>
</tbody>
</table>
<h2 id="_3">关键概念</h2>
<ol>
<li><strong>OTA（Over-The-Air）</strong>：物联网设备远程固件更新和配置下发协议，本文中用于设备注册和获取通信凭据</li>
<li><strong>Opus 编解码</strong>：开源音频编解码器，适合实时语音通信，16kHz/60ms 帧是低延迟配置</li>
<li><strong>Web Codecs API</strong>：Chromium 原生音视频编解码接口，支持硬件加速，比 WebAssembly 方案延迟更低</li>
<li><strong>ScriptProcessor vs AudioWorklet</strong>：浏览器音频处理 API，前者即将废弃但兼容性好，后者是未来标准</li>
<li><strong>AEC（Acoustic Echo Cancellation）</strong>：回声消除技术，在同时录音和播放时剔除扬声器输出</li>
<li><strong>SimDeviceBridge</strong>：作者设计的中间桥接层，解决浏览器无法设置自定义 WebSocket Header 和 CORS 限制的问题</li>
</ol>
<h2 id="_4">能力边界</h2>
<ul>
<li>仅适用于小智（XiaoZhi）平台的特定协议，不可直接复用于其他 IoT 设备</li>
<li>Web Codecs API 仅 Chromium 系浏览器支持（Chrome/Edge），Firefox/Safari 不兼容</li>
<li>模拟器无法完全替代真实硬件测试（如射频性能、功耗、延迟抖动等物理层问题）</li>
<li>Concentus（C# Opus）编码质量可能低于原生 Opus 库</li>
<li>需要部署 ASP.NET Core 桥接服务，增加了基础设施复杂度</li>
</ul>
<h2 id="_5">最佳实践</h2>
<ol>
<li><strong>浏览器模拟 IoT 设备是可行的</strong>——通过中间桥接层绕过浏览器安全限制</li>
<li><strong>音频编解码优先用原生 API</strong>（Web Codecs &gt; WebAssembly &gt; JS 纯实现）</li>
<li><strong>Opus 60ms 帧是语音通信的良好平衡点</strong>——延迟与质量的折中</li>
<li><strong>情感系统保持"说话中表情"</strong>——小细节大幅提升用户体验</li>
<li><strong>设备协议分析先行</strong>——先抓包理解 OTA/WebSocket 协议，再写模拟器</li>
</ol>
<h2 id="_6">🔍 借鉴学习进化点（必填）</h2>
<table>
<thead>
<tr>
<th>#</th>
<th>点</th>
<th>原文要点</th>
<th>对当前系统的借鉴</th>
<th>行动计划</th>
</tr>
</thead>
<tbody>
<tr>
<td>1</td>
<td>浏览器模拟硬件设备</td>
<td>用 Web Codecs + WebSocket 桥接层在浏览器里模拟 ESP32</td>
<td>ESP32 智能灯项目可参考此思路，用浏览器做虚拟设备调试</td>
<td>在 esp32-smart-lamp skill 中加入浏览器模拟调试方案</td>
</tr>
<tr>
<td>2</td>
<td>三层桥接架构</td>
<td>浏览器无法设自定义 Header → 中间 C# 代理转发</td>
<td>Agent 系统对外 API 调用遇到类似 CORS/Header 限制时，可用同样的桥接模式</td>
<td>记录此模式到 HTTP 调试经验库</td>
</tr>
<tr>
<td>3</td>
<td>Web Codecs API 硬件加速</td>
<td>Chromium 原生 AudioDecoder 比 WASM 延迟低一个数量级</td>
<td>涉及浏览器端音频处理时优先选 Web Codecs</td>
<td>Voicebox 前端如果做音频处理，优先用 Web Codecs</td>
</tr>
<tr>
<td>4</td>
<td>情感系统保持说话中表情</td>
<td>TTS 播放时不切换到扬声器图标，保持 LLM 情感</td>
<td>Agent 通知/状态展示也应保持上下文一致性，不因状态切换丢失信息</td>
<td>审查 Hermes TUI/dashboard 状态切换逻辑</td>
</tr>
<tr>
<td>5</td>
<td>timestamp 严格递增陷阱</td>
<td>AudioDecoder 要求微秒级 timestamp 严格递增</td>
<td>WebSocket/流式数据处理中 timestamp 管理是通用陷阱</td>
<td>检查 Voicebox 音频流 timestamp 处理是否有类似问题</td>
</tr>
</tbody>
</table>
<h2 id="rag-wiki">🔗 RAG / Wiki 联动（必填）</h2>
<h3 id="_7">当前状态</h3>
<ul>
<li>ESP32 智能灯项目（in_progress）：gesture_task.c / led_effect.c 开发中</li>
<li>Voicebox 前端架构 skill 已建立</li>
<li>WebSocket 相关技术经验已有积累</li>
</ul>
<h3 id="_8">改进方案</h3>
<ol>
<li>此文 OTA 协议分析 + 桥接架构可直接复用于 ESP32 项目调试</li>
<li>Web Codecs 音频处理经验可补充到 Voicebox 前端架构 skill</li>
<li>"浏览器模拟硬件"模式可作为通用调试方法论沉淀</li>
</ol>
<h3 id="_9">已执行操作</h3>
<ul>
<li>✅ 写入 MD 笔记</li>
<li>⏳ 生成 HTML 双格式</li>
<li>⏳ 更新 index.md</li>
</ul>
<h2 id="_10">关联</h2>
<ul>
<li><a href="esp32-smart-lamp.md">ESP32 智能灯开发工作流</a>（skill）</li>
<li><a href="voicebox-frontend-architecture.md">Voicebox 前端架构模式</a>（skill）</li>
<li><a href="2026-06-05-OrcaLab-虎鲸研究站.md">2026-06-05-OrcaLab-虎鲸研究站</a>（同日笔记）</li>
</ul>
<h2 id="_11">相关笔记</h2>
</body>
</html>