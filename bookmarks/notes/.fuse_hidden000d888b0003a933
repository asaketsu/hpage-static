<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Agent记忆自进化2026-MemRL运行时学习与三阶段框架</title>

<meta property="og:title" content="Agent记忆自进化2026-MemRL运行时学习与三阶段框架" />
<meta property="og:type" content="article" />
<meta property="og:description" content="笔记 | academic" />
<meta property="og:image" content="https://hpage.top/homepage/covers/2026-07-04-Agent%E8%AE%B0%E5%BF%86%E8%87%AA%E8%BF%9B%E5%8C%962026-MemRL%E8%BF%90%E8%A1%8C%E6%97%B6%E5%AD%A6%E4%B9%A0%E4%B8%8E%E4%B8%89%E9%98%B6%E6%AE%B5%E6%A1%86%E6%9E%B6.png" />

<style>
@import url('https://fonts.googleapis.com/css2?family=Noto+Sans+SC:wght@400..700&family=Noto+Serif+SC:wght@400..700&family=Space+Grotesk:wght@400..700&family=JetBrains+Mono:wght@400..500&family=IBM+Plex+Sans:wght@400..600&display=swap');

* { box-sizing: border-box; }

body {
  font-family: 'Noto Serif SC', 'Source Serif 4', Georgia, serif;
  max-width: 840px;
  margin: 0 auto;
  padding: 32px 24px;
  line-height: 1.85;
  font-weight: 450;
  color: #2c2c2c;
  background: #faf8f5;
}

h1 {
  font-family: 'Noto Serif SC', 'Playfair Display', Georgia, serif;
  font-size: 1.75em;
  font-weight: 700;
  color: #8b1a1a;
  border-bottom: none;
  padding-bottom: 0;
  margin-top: 0;
  margin-bottom: 0.6em;
}

h2 {
  font-family: 'Noto Serif SC', 'Playfair Display', Georgia, serif;
  font-size: 1.35em;
  font-weight: 600;
  color: #8b1a1a;
  margin-top: 1.8em;
  margin-bottom: 0.5em;
  border-bottom: 1px solid #e8e0d4;
  padding-bottom: 6px;
}

h3 {
  font-family: 'Noto Serif SC', 'Playfair Display', Georgia, serif;
  font-size: 1.12em;
  font-weight: 600;
  color: #8b1a1acc;
  margin-top: 1.4em;
}

h4 {
  font-size: 1em;
  font-weight: 600;
  color: #8b1a1aaa;
}

a {
  color: #8b4513;
  text-decoration: none;
  border-bottom: 1px solid #8b451330;
  transition: border-color 0.2s;
}

a:hover {
  border-bottom-color: #8b4513;
}

code {
  background: #f7f5f0;
  color: #9a5b13;
  padding: 2px 7px;
  border-radius: 4px;
  font-size: 0.88em;
  font-family: 'JetBrains Mono', 'SF Mono', 'Fira Code', monospace;
}

pre {
  background: #3c3226;
  color: #f5f0e8;
  border: none;
  padding: 20px 24px;
  border-radius: 8px;
  overflow-x: auto;
  line-height: 1.6;
  font-size: 0.88em;
  margin: 1.2em 0;
  box-shadow: 0 2px 8px rgba(0,0,0,0.08);
}

pre code {
  background: none;
  color: inherit;
  padding: 0;
  font-size: inherit;
}

blockquote {
  border-left: 4px solid #a0522d;
  margin: 1.2em 0;
  padding: 12px 20px;
  color: #2c2c2ccc;
  background: #f5f0e8;
  border-radius: 0 6px 6px 0;
}

/* ═══ table：彻底禁用 ═══ */
table, th, td { display: none !important; }

img {
  max-width: 100%;
  border-radius: 8px;
  margin: 8px 0;
  box-shadow: 0 1px 4px rgba(0,0,0,0.06);
}

hr {
  border: none;
  height: 1px;
  background: #e8e0d4;
  margin: 2.5em 0;
}

ul, ol {
  padding-left: 1.6em;
}

li { margin-bottom: 4px; }

strong { color: #8b1a1a; }


        h1 { font-style: italic; }
        h1::before { content: '§ '; color: #a0522d40; font-size: 0.7em; vertical-align: middle; }
        

/* 响应式 */
@media (max-width: 640px) {
  body { padding: 20px 16px; }
  h1 { font-size: 1.45em; }
  h2 { font-size: 1.2em; }
  pre { padding: 14px 16px; font-size: 0.82em; }
}

/* 打印优化 */
@media print {
  body { background: white; color: black; max-width: 100%; }
  pre { border: 1px solid #ddd; box-shadow: none; }
  a { color: #333; border-bottom: none; }
}
</style>
</head>
<body>
<h1 id="agent-2026memrl">Agent 记忆自进化 2026：MemRL 运行时学习与三阶段进化框架</h1>
<blockquote>
<p><strong>学习日期</strong>: 2026-07-04<br />
<strong>信息源</strong>: MemRL (arXiv:2601.03192v2), "From Storage to Experience" 综述 (Preprints 202601.0618), Mem0 State of AI Agent Memory 2026, SSGM 框架 (arXiv:2603.11768), OpenReview Experience-Driven Evolution Benchmark<br />
<strong>核心命题</strong>: Agent 记忆从"被动存储"进化到"运行时 RL 驱动的主动经验积累"，解决稳定性-可塑性两难</p>
</blockquote>
<hr />
<h2 id="_1">一、范式转变：记忆不再是"对话历史的副本"</h2>
<h3 id="11-2023-vs-2026">1.1 2023 vs 2026 的记忆观</h3>
<table>
<thead>
<tr>
<th>维度</th>
<th>2023 (旧范式)</th>
<th>2026 (新范式)</th>
</tr>
</thead>
<tbody>
<tr>
<td>记忆是什么</td>
<td>塞进上下文窗口的对话历史</td>
<td>一等架构组件，有自己的基准和文献</td>
</tr>
<tr>
<td>检索方式</td>
<td>被动语义相似度匹配</td>
<td>主动价值感知检索 (Value-Aware Retrieval)</td>
</tr>
<tr>
<td>更新方式</td>
<td>静态追加</td>
<td>运行时 RL 优化，"记住什么有效"</td>
</tr>
<tr>
<td>评估</td>
<td>自报告或临时任务</td>
<td>标准化基准 (LoCoMo/LongMemEval/BEAM)</td>
</tr>
<tr>
<td>核心矛盾</td>
<td>容量 vs 成本</td>
<td>稳定性 vs 可塑性 (Stability-Plasticity Dilemma)</td>
</tr>
</tbody>
</table>
<h3 id="12-2026">1.2 三大标准化基准 (2026 现状)</h3>
<p><strong>LoCoMo</strong> (Snap Research):<br />
- 1,540 道题，4 类：单跳、多跳、开放域、时序记忆<br />
- 测试跨多轮对话的记忆召回<br />
- 当前 SOTA: 92.5 分</p>
<p><strong>LongMemEval</strong>:<br />
- 500 道题，6 类：单会话用户/助手/偏好召回、知识更新、时序推理、多会话召回<br />
- 对知识更新和多会话任务要求极高<br />
- 当前 SOTA: 94.4 分</p>
<p><strong>BEAM</strong> (最重要):<br />
- 100 万和 1000 万 token 规模<br />
- 10 类：偏好跟随、指令跟随、信息提取、知识更新、多会话推理、摘要、时序推理、事件排序、弃权、矛盾解决<br />
- <strong>无法靠扩大上下文窗口解决</strong> → 最贴近生产环境<br />
- 评估五个维度：BLEU、F1、LLM Score、Token 消耗、延迟</p>
<blockquote>
<p><strong>核心洞察</strong>: 一个准确率高但每查询消耗 26,000 token 的系统不可投产。基准强制在准确率、成本、延迟间平衡。</p>
</blockquote>
<hr />
<h2 id="memrl">二、MemRL：运行时强化学习驱动记忆自进化</h2>
<blockquote>
<p>来源: arXiv:2601.03192v2 (2026-01)</p>
</blockquote>
<h3 id="21">2.1 核心创新：冻结模型 + 动态记忆的解耦</h3>
<pre><code>传统方式:                    MemRL 方式:
┌─────────────┐              ┌─────────────┐
│  LLM 参数   │ ◄── 微调     │  LLM (冻结) │ ←── 不动！
│  (全部更新) │              └──────┬──────┘
└─────────────┘                     │
   灾难性遗忘 ↑                     │ 检索
                                    ▼
                              ┌─────────────┐
                              │ 情景记忆    │ ←── 运行时 RL 更新
                              │ (动态进化)  │     无需梯度，无遗忘
                              └─────────────┘
</code></pre>
<p><strong>关键设计决策</strong>: 把"学什么"和"推理什么"完全分开。<br />
- LLM 推理能力保持稳定（冻结参数）<br />
- 记忆系统持续进化（非参数化 RL）<br />
- 彻底解决 stability-plasticity 两难</p>
<h3 id="22-intent-experience-utility">2.2 Intent-Experience-Utility 记忆结构</h3>
<p>每条记忆不再是简单的文本，而是三元组：</p>
<table>
<thead>
<tr>
<th>字段</th>
<th>含义</th>
<th>作用</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>Intent (zᵢ)</strong></td>
<td>用户查询/当前状态的嵌入表示</td>
<td>检索时的匹配锚点</td>
</tr>
<tr>
<td><strong>Experience (eᵢ)</strong></td>
<td>原始历史经验（解决路径或交互轨迹）</td>
<td>实际可复用的知识</td>
</tr>
<tr>
<td><strong>Utility (Qᵢ)</strong></td>
<td>学到的标量值，近似该经验的期望回报</td>
<td>决定"是否值得检索"</td>
</tr>
</tbody>
</table>
<h3 id="23">2.3 两阶段检索机制</h3>
<p><strong>Phase A — 相似度召回</strong>:</p>
<pre><code class="language-python"># 先用语义相似度做粗筛
candidates = {m for m in Memory if cosine_similarity(state, m.intent) &gt; δ}
</code></pre>
<p><strong>Phase B — 价值感知精排</strong>:</p>
<pre><code class="language-python"># 再用 RL 学到的 Q 值做精排
score = (1-λ) * normalized_similarity(state, m.intent) + λ * normalized_Q(m)
# λ 控制相似度 vs 效用的权衡
</code></pre>
<blockquote>
<p><strong>对比 RAG</strong>: RAG 只做 Phase A（语义相似度），经常检索到"语义相关但无用"的噪声。MemRL 的 Phase B 用 RL 学到的价值函数过滤掉"听起来对但实际没用"的记忆。</p>
</blockquote>
<h3 id="24-q">2.4 非参数化 Q 值更新</h3>
<pre><code class="language-python"># Monte Carlo 风格的 Q 值更新，无需梯度
Q_new ← Q_old + α · (r - Q_old)
# r = 实际任务回报（成功=1, 失败=0, 部分=中间值）
# α = 学习率
</code></pre>
<p>这让 Agent <strong>记住什么有效</strong>，而不是靠微调参数。</p>
<h3 id="25">2.5 实验结果亮点</h3>
<table>
<thead>
<tr>
<th>指标</th>
<th>MemRL</th>
<th>基线</th>
<th>提升</th>
</tr>
</thead>
<tbody>
<tr>
<td>累积成功率 (CSR)</td>
<td>—</td>
<td>—</td>
<td><strong>+3.8%</strong></td>
</tr>
<tr>
<td>探索密集环境 CSR</td>
<td>—</td>
<td>—</td>
<td><strong>+6.2%</strong></td>
</tr>
<tr>
<td>未见任务成功率</td>
<td>—</td>
<td>—</td>
<td><strong>+2.8%</strong></td>
</tr>
<tr>
<td>遗忘率</td>
<td>0.041</td>
<td>0.051</td>
<td><strong>-19.6%</strong></td>
</tr>
<tr>
<td>Q 值与成功率相关性</td>
<td>Pearson r=0.861</td>
<td>—</td>
<td>Q 值可靠反映经验质量</td>
</tr>
</tbody>
</table>
<p><strong>跨模型迁移</strong> (最惊艳的结果):<br />
- Gemini-3-Pro 积累的记忆迁移到 Qwen3-235B → <strong>&gt;3x 性能提升</strong><br />
- 说明高能力模型积累的经验可以"教学"弱模型，无需重新训练</p>
<p><strong>保留"失败记忆"的价值</strong>:<br />
- 保留约 12% 的"失败"记忆<br />
- 这些 near-miss 经验捕获了"差一点就成功"的策略信息<br />
- 删掉它们反而降低性能</p>
<hr />
<h2 id="storage-reflection-experience">三、三阶段进化框架：Storage → Reflection → Experience</h2>
<blockquote>
<p>来源: "From Storage to Experience" 综述 (Preprints 202601.0618, 2026-01)</p>
</blockquote>
<h3 id="31">3.1 统一形式化</h3>
<p>Agent 决策被形式化为：</p>
<pre><code>a_t ~ π_θ(a_t | I, o_t, m_t)

其中:
  I = 静态系统指令
  o_t = 当前观测
  m_t = Retrieve(M, o_t)  ← 上下文相关记忆
</code></pre>
<h3 id="32">3.2 三阶段进化</h3>
<pre><code>┌──────────────────────────────────────────────────────────────┐
│                   记忆进化三阶段框架                          │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  Stage 1: STORAGE (存储)                                    │
│  ──────────────────────                                      │
│  保留历史轨迹，最小变换                                      │
│  M_raw = {τ₁, τ₂, ..., τ_N}                                │
│                                                              │
│  方法: 线性(FIFO) | 向量 | 结构化(DB/图/层级)              │
│  特点: 一对一映射，无信息损失，但无抽象                     │
│                                                              │
│              ▼ (加入反馈信号)                                │
│                                                              │
│  Stage 2: REFLECTION (反思)                                 │
│  ──────────────────────                                      │
│  从被动记录器变为主动评论员                                  │
│  m'_i = F_ref(τ_i | φ)                                      │
│                                                              │
│  方法: 内省(自我纠错) | 环境(外部信号) | 协调(多Agent)      │
│  特点: 评估和修正推理路径，但仍是单轨迹处理                 │
│                                                              │
│              ▼ (跨轨迹抽象)                                  │
│                                                              │
│  Stage 3: EXPERIENCE (经验)                                 │
│  ──────────────────────                                      │
│  最高认知层，跨轨迹抽象提取行为模式                          │
│  K = F_exp(T_batch)  s.t.  |K| ≪ Σ|τ|                      │
│                                                              │
│  方法: 显式(可读规则) | 隐式(模型权重) | 混合(循环)         │
│  特点: 满足最小描述长度(MDL)原理，压缩冗余                 │
│                                                              │
└──────────────────────────────────────────────────────────────┘
</code></pre>
<h3 id="33-experience">3.3 Experience 阶段的关键机制</h3>
<p><strong>主动探索 (Active Exploration)</strong>:<br />
- 从"被动记录器"变为"主动经验收集器"<br />
- 驱动方式：基于奖励、基于课程、基于复用<br />
- 探索维度：广度（补认知缺陷）、深度（提取高阶技能）、策略（优化决策路径）</p>
<p><strong>跨轨迹抽象 (Cross-Trajectory Abstraction)</strong>:<br />
- <strong>对比归纳</strong>：对比成功 vs 失败轨迹，提取差异模式<br />
- <strong>分块聚合</strong>：跨粒度级别分块和聚合<br />
- <strong>行为模式封装</strong>：将重复模式封装为可复用函数<br />
- <strong>内化</strong>：最终可内化到模型参数中</p>
<p><strong>抽象粒度</strong>:<br />
| 粒度 | 形式 | 示例 |<br />
|------|------|------|<br />
| 浅层 | 自然语言规则 | "处理文件路径时总是用绝对路径" |<br />
| 中层 | 模块化执行骨架 | 标准化的调试流程模板 |<br />
| 深层 | 模型权重 | 微调后的参数 |</p>
<hr />
<h2 id="2026">四、三大开放难题 (2026 现状)</h2>
<blockquote>
<p>来源: Mem0 State of AI Agent Memory 2026 报告</p>
</blockquote>
<h3 id="41-cross-session-identity">4.1 跨会话身份 (Cross-Session Identity)</h3>
<ul>
<li>用户在 Session A 说"我叫张三"，Session B 能否记住？</li>
<li>当前大多数系统无法跨 session 维持用户身份</li>
<li>核心挑战：身份信息分散在不同记忆片段中，需要全局聚合</li>
</ul>
<h3 id="42-temporal-abstraction-at-scale">4.2 大规模时序抽象 (Temporal Abstraction at Scale)</h3>
<ul>
<li>"上周用户说了X" vs "三个月前用户说了X" — 时序衰减如何建模？</li>
<li>BEAM 基准在 1M-10M token 规模下，时序推理仍是最大失分项</li>
<li>需要：时间感知的索引 + 衰减函数 + 周期性整理</li>
</ul>
<h3 id="43-memory-staleness">4.3 记忆陈旧 (Memory Staleness)</h3>
<ul>
<li>环境变化导致记忆过时（API 改版、工具升级、密码更换）</li>
<li>SSGM 框架 (arXiv:2603.11768) 指出：<strong>动态记忆的错误是累积且持久的</strong>，不像静态 RAG 可回滚</li>
<li>需要：一致性验证 + 时间衰减 + 动态访问控制</li>
</ul>
<hr />
<h2 id="hermes">五、对 Hermes 系统的实践启发</h2>
<h3 id="51">5.1 立即可采纳的改进</h3>
<p><strong>① 记忆 Utility 值追踪</strong> (参考 MemRL):<br />
- 给每条 Memory 增加 <code>utility_score</code> 字段<br />
- 每次被检索且帮助完成任务 → utility += α·(1 - utility)<br />
- 每次被检索但未帮助 → utility += α·(0 - utility)<br />
- 定期淘汰 utility &lt; 阈值的记忆</p>
<p><strong>② 两阶段检索</strong> (参考 MemRL Phase A + B):<br />
- 当前 <code>memory</code> 工具只做语义相似度检索<br />
- 增加 Phase B：在候选集中按 utility_score 重排<br />
- 预期减少"语义相关但无用"的记忆噪声</p>
<p><strong>③ 失败经验保留</strong> (参考 MemRL 12% 失败记忆):<br />
- 当前 <code>error-case-library</code> Skill 只记录"修复成功"的案例<br />
- 应同时保留"尝试了但没解决"的 near-miss 经验<br />
- 这些经验捕获了"差一点就行"的策略信息</p>
<p><strong>④ 三阶段记忆审计</strong> (参考 Storage→Reflection→Experience):<br />
- 定期检查 Memory：哪些还停在 Storage 阶段（原始记录无抽象）？<br />
- 用 Reflection 机制（如 <code>agent-reflexion-workflow</code>）将原始记录转为评估过的经验<br />
- 用 Experience 机制（如 <code>evolver</code>）将多条经验压缩为通用规则</p>
<h3 id="52-skill">5.2 Skill 改进建议</h3>
<table>
<thead>
<tr>
<th>Skill</th>
<th>当前状态</th>
<th>改进方向</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>memory-consolidation</code></td>
<td>基于时间/access_count 衰减</td>
<td>增加 utility_score 追踪，参考 MemRL Q 值</td>
</tr>
<tr>
<td><code>memory-write</code></td>
<td>写入即固定</td>
<td>增加 utility_score 初始值，后续动态更新</td>
</tr>
<tr>
<td><code>agent-reflexion-workflow</code></td>
<td>单任务反思</td>
<td>扩展为跨轨迹对比归纳（成功 vs 失败）</td>
</tr>
<tr>
<td><code>evolver</code></td>
<td>错误→规则</td>
<td>增加 Experience 阶段的 MDL 压缩，合并冗余规则</td>
</tr>
<tr>
<td><code>auto-memory-keeper</code></td>
<td>每小时捕获亮点</td>
<td>增加"这条记忆对谁有用"的价值评估</td>
</tr>
</tbody>
</table>
<h3 id="53">5.3 跨模型记忆迁移的启示</h3>
<p>MemRL 最惊艳的结果：<strong>Gemini-3-Pro 的记忆迁移给 Qwen3-235B → 3x 提升</strong>。</p>
<p>对 Hermes 的意义：<br />
- 不同 Hermes 实例（jbot/ubot/abot）使用不同模型<br />
- 如果 jbot（强模型）积累的经验能结构化迁移给 abot（弱模型）<br />
- 这比各自从零学习高效得多<br />
- 已有 <code>exchange-protocol</code> Skill 做 JSONL 经验池共享，可升级为 utility-aware 的经验迁移</p>
<hr />
<h2 id="_2">六、知识缺口与下一步</h2>
<ol>
<li><strong>MemRL 的 λ 参数调优</strong>：相似度 vs 效用的权衡参数，论文未给最优值 → 需在自己的任务上实验</li>
<li><strong>Experience 抽象的自动化</strong>：从多条轨迹自动提取规则，目前靠 LLM prompt，质量不稳定 → 可探索聚类+模板</li>
<li><strong>记忆治理与安全的工程化</strong>：SSGM 提出了框架但缺少参考实现 → 可参考其设计实现一个 <code>memory-governance</code> 模块</li>
<li><strong>BEAM 级别的大规模测试</strong>：Hermes 记忆系统从未在 1M+ token 规模测试过 → 需要压力测试</li>
</ol>
<hr />
<h2 id="_3">附录：信息源</h2>
<ol>
<li>MemRL — arXiv:2601.03192v2 (2026-01): 运行时 RL 在情景记忆上实现自进化，非参数化</li>
<li>"From Storage to Experience" — Preprints 202601.0618 (2026-01): LLM Agent 记忆机制三阶段进化综述</li>
<li>Mem0 State of AI Agent Memory 2026 — 三大基准 (LoCoMo/LongMemEval/BEAM) + 三大开放难题</li>
<li>SSGM Framework — arXiv:2603.11768v1: Stability and Safety-Governed Memory，动态记忆治理</li>
<li>OpenReview Experience-Driven Evolution Benchmark: 经验驱动自进化 Agent 基准</li>
</ol>
<hr />
<p><strong>笔记字数</strong>: ~3200 字 | <strong>核心发现</strong>: 5 个 | <strong>信息源</strong>: 5 个</p>
<hr />
<h2 id="_4">相关笔记</h2>
<ul>
<li>[[2026-05-01-AI-Agent记忆架构前沿学习]] — Agent 记忆架构综述，本文聚焦 2026 运行时 RL 进化</li>
<li>[[2026-05-04-个人Agent自进化五步闭环]] — 自进化五步闭环，与三阶段框架对照</li>
<li>[[2026-05-22-Hermes-Agent四层记忆架构深度分析]] — Hermes 四层记忆实践，理论落地参考</li>
<li>[[2026-06-14-ai-agent-memory-architecture-2026]] — 2026 记忆架构全景，MemRL 运行时学习的上位概念</li>
</ul>
<hr />
</body>
</html>