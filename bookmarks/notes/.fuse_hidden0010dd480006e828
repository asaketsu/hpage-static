<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>SkillOpt-微软Agent技能训练优化器</title>

<meta property="og:title" content="SkillOpt-微软Agent技能训练优化器" />
<meta property="og:type" content="article" />
<meta property="og:description" content="笔记 | security" />
<meta property="og:image" content="https://hpage.top/homepage/covers/2026-08-06-SkillOpt-%E5%BE%AE%E8%BD%AFAgent%E6%8A%80%E8%83%BD%E8%AE%AD%E7%BB%83%E4%BC%98%E5%8C%96%E5%99%A8.png" />

<style>
@import url('https://fonts.googleapis.com/css2?family=Noto+Sans+SC:wght@400..700&family=Noto+Serif+SC:wght@400..700&family=Space+Grotesk:wght@400..700&family=JetBrains+Mono:wght@400..500&family=IBM+Plex+Sans:wght@400..600&display=swap');

* { box-sizing: border-box; }

body {
  font-family: 'Fira Code', 'JetBrains Mono', monospace;
  max-width: 840px;
  margin: 0 auto;
  padding: 32px 24px;
  line-height: 1.85;
  font-weight: 450;
  color: #1f2937;
  background: #f3f4f6;
}

h1 {
  font-family: 'JetBrains Mono', 'Space Mono', monospace;
  font-size: 1.75em;
  font-weight: 700;
  color: #111827;
  border-bottom: none;
  padding-bottom: 0;
  margin-top: 0;
  margin-bottom: 0.6em;
}

h2 {
  font-family: 'JetBrains Mono', 'Space Mono', monospace;
  font-size: 1.35em;
  font-weight: 600;
  color: #111827;
  margin-top: 1.8em;
  margin-bottom: 0.5em;
  border-bottom: 1px solid #e5e7eb;
  padding-bottom: 6px;
}

h3 {
  font-family: 'JetBrains Mono', 'Space Mono', monospace;
  font-size: 1.12em;
  font-weight: 600;
  color: #111827cc;
  margin-top: 1.4em;
}

h4 {
  font-size: 1em;
  font-weight: 600;
  color: #111827aa;
}

a {
  color: #dc2626;
  text-decoration: none;
  border-bottom: 1px solid #dc262630;
  transition: border-color 0.2s;
}

a:hover {
  border-bottom-color: #dc2626;
}

code {
  background: #fef2f2;
  color: #dc2626;
  padding: 2px 7px;
  border-radius: 4px;
  font-size: 0.88em;
  font-family: 'JetBrains Mono', 'SF Mono', 'Fira Code', monospace;
}

pre {
  background: #111827;
  color: #f9fafb;
  border: none;
  padding: 20px 24px;
  border-radius: 8px;
  overflow-x: auto;
  line-height: 1.6;
  font-size: 0.88em;
  margin: 1.2em 0;
  box-shadow: 0 2px 8px rgba(0,0,0,0.08);
}

pre code {
  background: none;
  color: inherit;
  padding: 0;
  font-size: inherit;
}

blockquote {
  border-left: 4px solid #dc2626;
  margin: 1.2em 0;
  padding: 12px 20px;
  color: #1f2937cc;
  background: #fef2f2;
  border-radius: 0 6px 6px 0;
}

/* ═══ table：彻底禁用 ═══ */
table, th, td { display: none !important; }

img {
  max-width: 100%;
  border-radius: 8px;
  margin: 8px 0;
  box-shadow: 0 1px 4px rgba(0,0,0,0.06);
}

hr {
  border: none;
  height: 1px;
  background: #e5e7eb;
  margin: 2.5em 0;
}

ul, ol {
  padding-left: 1.6em;
}

li { margin-bottom: 4px; }

strong { color: #111827; }


        h1::before { content: '🛡 '; }
        pre { border-left: 3px solid #dc2626; }
        

/* 响应式 */
@media (max-width: 640px) {
  body { padding: 20px 16px; }
  h1 { font-size: 1.45em; }
  h2 { font-size: 1.2em; }
  pre { padding: 14px 16px; font-size: 0.82em; }
}

/* 打印优化 */
@media print {
  body { background: white; color: black; max-width: 100%; }
  pre { border: 1px solid #ddd; box-shadow: none; }
  a { color: #333; border-bottom: none; }
}
</style>
</head>
<body>
<h1 id="skillopt-ai-agent">SkillOpt — 微软开源的 AI Agent 技能训练优化器</h1>
<blockquote>
<p><strong>来源</strong>：<a href="https://github.com/microsoft/SkillOpt">github.com/microsoft/SkillOpt</a> · <a href="https://microsoft.github.io/SkillOpt/">项目页</a> · <a href="https://arxiv.org/abs/2605.23904">arXiv:2605.23904</a><br />
<strong>论文</strong>：SkillOpt: Executive Strategy for Self-Evolving Agent Skills (2026年5月)<br />
<strong>机构</strong>：Microsoft Research<br />
<strong>License</strong>：MIT<br />
<strong>PyPI</strong>：<code>pip install skillopt</code>（Python 3.10+）</p>
</blockquote>
<hr />
<h2 id="_1">一句话定义</h2>
<blockquote>
<p>SkillOpt 把 AI Agent 的 <strong>skill.md 文档当作可训练参数</strong>，用类似神经网络训练的方法（epochs、batch size、learning rate、validation gate）在文本空间优化它——<strong>不动模型权重，只训练技能文档</strong>。</p>
</blockquote>
<hr />
<h2 id="_2">内容整理</h2>
<h3 id="_3">核心思想：训练过程知识，而非模型权重</h3>
<p>AI Agent 的瓶颈不再是"知识"（模型权重里已经有了），而是<strong>过程能力</strong>（procedural capability）——工具调用、中间状态检查、领域惯例、失败恢复。</p>
<p>传统 Agent 技能的三种来源都有问题：</p>
<table>
<thead>
<tr>
<th>方式</th>
<th>问题</th>
</tr>
</thead>
<tbody>
<tr>
<td>专家手写</td>
<td>依赖经验，无法自动化</td>
</tr>
<tr>
<td>LLM 一次性生成</td>
<td>无验证，容易跑偏</td>
</tr>
<tr>
<td>自由自修订</td>
<td>缺乏优化纪律，技能越改越长，悄悄退化</td>
</tr>
</tbody>
</table>
<p>SkillOpt 的方案：<strong>把 skill.md 当作冻结模型外部的一个可训练参数，用深度学习优化的纪律来训练它</strong>。</p>
<h3 id="-">前向-反向-更新循环</h3>
<p>SkillOpt 刻意模拟学习算法的三个阶段：</p>
<pre><code>1. 前向传播 (Forward / Rollout)
   冻结的目标模型用当前 skill.md 执行一批训练任务
   → 记录消息、工具调用、验证反馈、最终得分

2. 反向传播 (Backward / Reflection)
   独立的优化器模型分析成功和失败的轨迹（分开做 minibatch）
   → 成功轨迹：提炼可复用的模式
   → 失败轨迹：找到需要修正的反复错误

3. 参数更新 (Update / Bounded Edits)
   优化器提出 add/delete/replace 编辑
   → 编辑被合并、去重、排序、按学习率裁剪
   → 通过验证门控：只有严格提升验证集得分才接受
   → 被拒绝的编辑进入 rejected-edit buffer（负反馈）
</code></pre>
<h3 id="_4">五个关键控制组件</h3>
<table>
<thead>
<tr>
<th>组件</th>
<th>类比</th>
<th>作用</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>Rollout batch size</strong></td>
<td>forward pass 的 batch size</td>
<td>控制每次更新收集多少证据</td>
</tr>
<tr>
<td><strong>Textual learning rate</strong></td>
<td>学习率</td>
<td>限制每步编辑量，防止有用规则被大重写覆盖</td>
</tr>
<tr>
<td><strong>Rejected-edit buffer</strong></td>
<td>负样本记忆</td>
<td>被拒绝的编辑作为负反馈传给后续优化器调用</td>
</tr>
<tr>
<td><strong>Slow / Meta update</strong></td>
<td>epoch 级慢更新</td>
<td>跨 epoch 整合长期教训（单 batch 看不到的）</td>
</tr>
<tr>
<td><strong>Validation gate</strong></td>
<td>held-out validation</td>
<td>严格单调提升——不通过就拒绝</td>
</tr>
</tbody>
</table>
<h3 id="best_skillmd">产出物：best_skill.md</h3>
<ul>
<li>紧凑：中位数 ~920 tokens（300-2000 范围）</li>
<li>可读：像资深从业者的建议</li>
<li>可审计：每条编辑有记录</li>
<li><strong>可迁移</strong>：跨模型规模、跨 Agent 框架、跨相近基准测试无需重训</li>
</ul>
<blockquote>
<p>最终技能里通常只有 1-4 条被接受的编辑。OfficeQA 的 +39.0 分提升来自<strong>单条被接受的编辑</strong>。</p>
</blockquote>
<h3 id="5252">实验结果：52/52 全胜</h3>
<p>6 个基准测试 × 7 个目标模型 × 3 种执行模式 = <strong>52 个评测单元，全部最佳或并列最佳</strong>。</p>
<table>
<thead>
<tr>
<th>目标模型</th>
<th>执行方式</th>
<th>平均提升</th>
</tr>
</thead>
<tbody>
<tr>
<td>GPT-5.5</td>
<td>Direct chat</td>
<td><strong>+23.5</strong></td>
</tr>
<tr>
<td>GPT-5.5</td>
<td>Codex CLI</td>
<td><strong>+24.8</strong></td>
</tr>
<tr>
<td>GPT-5.5</td>
<td>Claude Code</td>
<td><strong>+19.1</strong></td>
</tr>
<tr>
<td>GPT-5.4-mini</td>
<td>Direct chat</td>
<td>+12.7</td>
</tr>
<tr>
<td>GPT-5.4-nano</td>
<td>Direct chat</td>
<td>+24.9</td>
</tr>
<tr>
<td>GPT-5.2</td>
<td>Direct chat</td>
<td>+16.6</td>
</tr>
<tr>
<td>Qwen3.5-4B</td>
<td>Direct chat</td>
<td>+19.2</td>
</tr>
</tbody>
</table>
<p><strong>最大提升出现在过程性任务</strong>：<br />
- SpreadsheetBench: 41.8 → 80.7 (+38.9)<br />
- OfficeQA: 33.1 → 72.1 (+39.0)<br />
- LiveMathBench: 37.6 → 66.9 (+29.3)</p>
<h3 id="_5">小模型 + 优化技能 ≈ 大模型</h3>
<ul>
<li>GPT-5.4-mini + skill (64.3) &gt; GPT-5.4 无 skill 基线 (59.7)</li>
<li>GPT-5.4-nano + skill (57.4) &gt; GPT-5.2 无 skill 基线 (51.3)</li>
<li>Qwen3.5-4B + skill &gt; GPT-5.2 无 skill 基线</li>
</ul>
<blockquote>
<p>曾经需要更大模型才能实现的收益，现在一个优化的 skill 文件就能近似。</p>
</blockquote>
<h3 id="_6">跨框架迁移验证</h3>
<table>
<thead>
<tr>
<th>迁移方向</th>
<th>结果</th>
</tr>
</thead>
<tbody>
<tr>
<td>GPT-5.4 → GPT-5.4-nano (跨模型)</td>
<td>技能仍然有效</td>
</tr>
<tr>
<td>Codex 训练 → Claude Code (跨框架)</td>
<td>22.1 → 81.8 (+59.7)，略高于 Claude Code 内直接训练 (80.4)</td>
</tr>
<tr>
<td>GPT-5.4-nano 自优化</td>
<td>SpreadsheetBench 超越基线</td>
</tr>
</tbody>
</table>
<blockquote>
<p>跨框架迁移说明 SkillOpt 学到的是<strong>通用工作流逻辑</strong>，不是框架特定的配方。</p>
</blockquote>
<h3 id="_7">消融实验</h3>
<table>
<thead>
<tr>
<th>组件</th>
<th>移除后 SpreadsheetBench</th>
</tr>
</thead>
<tbody>
<tr>
<td>完整 SkillOpt</td>
<td><strong>77.5</strong></td>
</tr>
<tr>
<td>无 learning rate</td>
<td>75.7</td>
</tr>
<tr>
<td>无 rejected buffer</td>
<td>72.9</td>
</tr>
<tr>
<td>无 meta skill + slow update</td>
<td><strong>55.0</strong> (暴跌)</td>
</tr>
</tbody>
</table>
<h3 id="_8">工程架构</h3>
<table>
<thead>
<tr>
<th>组件</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>目标模型</strong></td>
<td>冻结的 LLM（GPT/Qwen/任意 OpenAI 兼容）</td>
</tr>
<tr>
<td><strong>优化器模型</strong></td>
<td>独立模型（论文用 GPT-5.5），分析轨迹提出编辑</td>
</tr>
<tr>
<td><strong>后端</strong></td>
<td><code>openai_chat</code> / <code>claude_chat</code> / <code>qwen_chat</code> / <code>openai_compatible</code> / <code>codex_exec</code> / <code>claude_code_exec</code> / <code>cursor_exec</code></td>
</tr>
<tr>
<td><strong>基准测试</strong></td>
<td><code>skillopt/envs/&lt;name&gt;/</code> 包含 adapter + data loader + 评分 helper</td>
</tr>
<tr>
<td><strong>WebUI</strong></td>
<td>Gradio 监控面板（默认端口 7860）</td>
</tr>
<tr>
<td><strong>Cursor 插件</strong></td>
<td><code>.cursor-plugin</code> 目录</td>
</tr>
</tbody>
</table>
<h3 id="_9">安装与使用</h3>
<pre><code class="language-bash"># 安装
pip install skillopt

# 文档
# https://github.com/microsoft/SkillOpt/blob/main/docs/index.md
</code></pre>
<hr />
<h2 id="_10">查询拓展</h2>
<ul>
<li>SkillOpt 的 textual learning rate 具体怎么度量"编辑量"？按 token 数还是编辑操作数？</li>
<li>rejected-edit buffer 的容量和淘汰策略是什么？</li>
<li>论文中 GPT-5.5 作为优化器的 API 成本是多少？一次完整训练需要多少次优化器调用？</li>
<li>best_skill.md 跨模型迁移时，有没有出现负面迁移（某模型上变差）的情况？</li>
<li>SkillOpt 的 validation gate 是否会过拟合验证集？validation split 怎么设计？</li>
<li>Hermes Agent 的 Skill 体系能否直接接入 SkillOpt？需要什么适配？</li>
<li><code>skillopt_sleep</code> 是什么？从目录名看像是"睡眠/低频训练"模式？</li>
</ul>
<hr />
<h2 id="_11">归纳总结</h2>
<p>SkillOpt 是一个范式转换级的工作——<strong>把"写 prompt"变成了"训练 skill"</strong>。</p>
<p>核心洞察：AI Agent 的过程知识（procedural knowledge）不一定非得存在模型权重里。一个紧凑的、可读的、可审计的 natural language skill 文档，同样可以作为"可训练参数"被系统化优化。而且这种优化：</p>
<ol>
<li><strong>不需要 GPU</strong> — 训练成本是优化器模型的 API 调用</li>
<li><strong>部署成本为零</strong> — skill.md 只是系统提示词的一部分</li>
<li><strong>可迁移</strong> — 跨模型、跨框架、跨任务</li>
<li><strong>可审计</strong> — 每条编辑有记录，rejected 有反馈</li>
<li><strong>单调提升</strong> — validation gate 保证不退化</li>
</ol>
<p>这意味着领域适配 Agent 的路径从"微调权重"转向"训练一个小的、可版本控制的 natural language 技能层"——<strong>只要存在自动评估或可靠的验证器</strong>。</p>
<hr />
<h2 id="_12">趋势判断</h2>
<ul>
<li><strong>Skill-as-Trainable-Parameter 范式确立</strong>：SkillOpt 证明了"模型冻结、训练外部技能"这条路走通了。微软同时推出 SkillLens（配套项目），说明这是系统性的研究方向</li>
<li><strong>小模型+优化技能&gt;大模型</strong>：GPT-5.4-nano + skill &gt; GPT-5.2 无 skill，意味着推理成本可以被技能优化大幅压缩</li>
<li><strong>Agent 训练不需要 GPU 的时代</strong>：text-space optimization 只需 API 调用，AI Agent 的"微调"门槛从 GPU 集群降到 API Key</li>
<li><strong>技能市场崛起</strong>：当 best_skill.md 可以迁移复用，技能本身成为可交易的数字资产（类似模型权重在 HuggingFace 上的地位）</li>
<li><strong>验证器（Verifier）成为关键基础设施</strong>：SkillOpt 的前提是"存在自动评估或可靠验证器"，验证器质量直接决定技能优化上限</li>
</ul>
<hr />
<h2 id="_13">关联分析</h2>
<ul>
<li><strong>与 Hermes Agent Skill 体系的直接关联</strong>：Hermes 有 180+ Skills，全部是手写或错误模式提取的。SkillOpt 可以<strong>自动化优化这些 Skill</strong>——用 Hermes 的 cron 执行历史作为 rollout 数据，用 glm-5.2 或更强模型作为优化器，自动迭代每个 Skill 的 SKILL.md。这是 Hermes 自我进化能力的直接升级路径</li>
<li><strong>与用户 evolver/error-case-library 系统的关联</strong>：用户已有的 evolver Skill（错误模式→规则沉淀）和 SkillOpt 是同一个思路的不同实现——evolver 是手工规则提取，SkillOpt 是系统化自动优化。两者可以融合</li>
<li><strong>与 auto-prompt-optimization 的关联</strong>：用户已有 OPRO/GEPA/Sprig 方法选型的 Skill。SkillOpt 是 GEPA（生成式提示优化）的工业级进化版——增加了 validation gate、rejected buffer、slow update 等稳定性控制</li>
<li><strong>与 FDE 商业模式的关联</strong>：SkillOpt 的跨框架迁移特性意味着——FDE 训练好的 best_skill.md 可以跨 Codex/Claude Code/Cursor 复用，一次训练多次交付</li>
<li><strong>与数字克隆的关联</strong>：用户的数字克隆项目需要建模用户的决策风格和行为模式，SkillOpt 可以把这种"过程知识"训练成可迁移的 skill 文档</li>
</ul>
<hr />
<h2 id="_14">📎 关键链接</h2>
<ul>
<li>📖 论文：<a href="https://arxiv.org/abs/2605.23904">arXiv:2605.23904</a></li>
<li>🏠 项目页：<a href="https://microsoft.github.io/SkillOpt/">microsoft.github.io/SkillOpt</a></li>
<li>💻 GitHub：<a href="https://github.com/microsoft/SkillOpt">github.com/microsoft/SkillOpt</a></li>
<li>📦 PyPI：<code>pip install skillopt</code></li>
<li>📰 微软博客：<a href="https://www.microsoft.com/en-us/research/blog/skillopt-agent-skills-as-trainable-parameters">SkillOpt: Agent skills as trainable parameters</a></li>
<li>🎬 Demo：<a href="https://youtu.be/JUBMDTCiM0M">YouTube</a></li>
</ul>
<hr />
<p><em>笔记创建：2026-08-06 · 来源：Microsoft Research · arXiv:2605.23904</em></p>
<h2 id="_15">相关笔记</h2>
<ul>
<li><a href="./explore/2026-08-06-SkillUse.md">Skill-Use Benchmark</a> — Skill 使用能力评估，SkillOpt 优化的验证靶标</li>
<li><a href="./2026-08-06-DeepSeek-Agent-Harness执行栈全景图.md">DeepSeek Agent Harness</a> — Harness 作为 SkillOpt 执行底座</li>
<li><a href="./2026-08-06-open-kimi-ppt-skill-AI-Agent生成PPT.md">open-kimi-ppt skill</a> — 具体 Skill 实现案例，SkillOpt 可优化对象</li>
<li><a href="./2026-08-01-自进化AI-Agent前沿-MANTA动态拓扑与Skill-Self-Play技能共进化.md">自进化 AI Agent 前沿 MANTA</a> — Skill 自进化理论框架</li>
</ul>
</body>
</html>