<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>liteparse</title>

<meta property="og:title" content="liteparse" />
<meta property="og:type" content="article" />
<meta property="og:description" content="笔记 | academic" />
<meta property="og:image" content="https://hpage.top/homepage/covers/2026-05-29-liteparse.png" />

<style>
@import url('https://fonts.googleapis.com/css2?family=Noto+Sans+SC:wght@400..700&family=Noto+Serif+SC:wght@400..700&family=Space+Grotesk:wght@400..700&family=JetBrains+Mono:wght@400..500&family=IBM+Plex+Sans:wght@400..600&display=swap');

* { box-sizing: border-box; }

body {
  font-family: 'Noto Serif SC', 'Source Serif 4', Georgia, serif;
  max-width: 840px;
  margin: 0 auto;
  padding: 32px 24px;
  line-height: 1.85;
  font-weight: 450;
  color: #2c2c2c;
  background: #faf8f5;
}

h1 {
  font-family: 'Noto Serif SC', 'Playfair Display', Georgia, serif;
  font-size: 1.75em;
  font-weight: 700;
  color: #8b1a1a;
  border-bottom: none;
  padding-bottom: 0;
  margin-top: 0;
  margin-bottom: 0.6em;
}

h2 {
  font-family: 'Noto Serif SC', 'Playfair Display', Georgia, serif;
  font-size: 1.35em;
  font-weight: 600;
  color: #8b1a1a;
  margin-top: 1.8em;
  margin-bottom: 0.5em;
  border-bottom: 1px solid #e8e0d4;
  padding-bottom: 6px;
}

h3 {
  font-family: 'Noto Serif SC', 'Playfair Display', Georgia, serif;
  font-size: 1.12em;
  font-weight: 600;
  color: #8b1a1acc;
  margin-top: 1.4em;
}

h4 {
  font-size: 1em;
  font-weight: 600;
  color: #8b1a1aaa;
}

a {
  color: #8b4513;
  text-decoration: none;
  border-bottom: 1px solid #8b451330;
  transition: border-color 0.2s;
}

a:hover {
  border-bottom-color: #8b4513;
}

code {
  background: #f7f5f0;
  color: #9a5b13;
  padding: 2px 7px;
  border-radius: 4px;
  font-size: 0.88em;
  font-family: 'JetBrains Mono', 'SF Mono', 'Fira Code', monospace;
}

pre {
  background: #3c3226;
  color: #f5f0e8;
  border: none;
  padding: 20px 24px;
  border-radius: 8px;
  overflow-x: auto;
  line-height: 1.6;
  font-size: 0.88em;
  margin: 1.2em 0;
  box-shadow: 0 2px 8px rgba(0,0,0,0.08);
}

pre code {
  background: none;
  color: inherit;
  padding: 0;
  font-size: inherit;
}

blockquote {
  border-left: 4px solid #a0522d;
  margin: 1.2em 0;
  padding: 12px 20px;
  color: #2c2c2ccc;
  background: #f5f0e8;
  border-radius: 0 6px 6px 0;
}

/* ═══ table：彻底禁用 ═══ */
table, th, td { display: none !important; }

img {
  max-width: 100%;
  border-radius: 8px;
  margin: 8px 0;
  box-shadow: 0 1px 4px rgba(0,0,0,0.06);
}

hr {
  border: none;
  height: 1px;
  background: #e8e0d4;
  margin: 2.5em 0;
}

ul, ol {
  padding-left: 1.6em;
}

li { margin-bottom: 4px; }

strong { color: #8b1a1a; }


        h1 { font-style: italic; }
        h1::before { content: '§ '; color: #a0522d40; font-size: 0.7em; vertical-align: middle; }
        

/* 响应式 */
@media (max-width: 640px) {
  body { padding: 20px 16px; }
  h1 { font-size: 1.45em; }
  h2 { font-size: 1.2em; }
  pre { padding: 14px 16px; font-size: 0.82em; }
}

/* 打印优化 */
@media print {
  body { background: white; color: black; max-width: 100%; }
  pre { border: 1px solid #ddd; box-shadow: none; }
  a { color: #333; border-bottom: none; }
}
</style>
</head>
<body>
<h1 id="liteparse-llamaindex">LiteParse — LlamaIndex 开源文档解析器</h1>
<blockquote>
<p><strong>来源</strong>：run-llama/liteparse · GitHub · 2026-02-09 创建<br />
<strong>原文</strong>：https://github.com/run-llama/liteparse<br />
<strong>格式</strong>：双格式(MD + HTML) · 含借鉴进化点 · RAG/wiki联动</p>
</blockquote>
<h2 id="_1">摘要</h2>
<p>LiteParse 是 LlamaIndex 团队推出的开源文档解析工具，用 Rust 编写，专注<strong>快速轻量</strong>的 PDF 及多格式文档解析。核心能力：基于 PDFium 的空间文本提取 + Tesseract 内置 OCR + 多语言绑定（Python/Node/WASM/Rust CLI）。6.4K Star，Apache 2.0 许可。不依赖云服务，全部本地运行。定位是 LlamaParse（云端付费版）的开源轻量替代。</p>
<h2 id="_2">核心内容</h2>
<h3 id="1">1. 架构设计</h3>
<p>Rust 核心引擎 + 多语言绑定架构：</p>
<ol>
<li><strong>格式转换层</strong>：LibreOffice（Office 文档）+ ImageMagick（图片）→ 统一转 PDF</li>
<li><strong>文本提取层</strong>：PDFium C 库（Google 的 PDF 渲染引擎）</li>
<li><strong>OCR 层</strong>：Tesseract 内置（零配置）/ HTTP OCR 服务器（EasyOCR、PaddleOCR、自定义）</li>
<li><strong>合并层</strong>：原生文本 + OCR 结果智能合并</li>
<li><strong>空间投影层</strong>：Grid Projection 重建空间布局，保留 bounding box 信息</li>
</ol>
<h3 id="2">2. 多格式输入支持</h3>
<table>
<thead>
<tr>
<th>类别</th>
<th>格式</th>
<th>依赖</th>
</tr>
</thead>
<tbody>
<tr>
<td>PDF</td>
<td>.pdf</td>
<td>内置</td>
</tr>
<tr>
<td>Word</td>
<td>.doc/.docx/.docm/.odt/.rtf/.pages</td>
<td>LibreOffice</td>
</tr>
<tr>
<td>PPT</td>
<td>.ppt/.pptx/.pptm/.odp/.key</td>
<td>LibreOffice</td>
</tr>
<tr>
<td>表格</td>
<td>.xls/.xlsx/.xlsm/.ods/.csv/.numbers</td>
<td>LibreOffice</td>
</tr>
<tr>
<td>图片</td>
<td>.jpg/.png/.gif/.bmp/.tiff/.webp/.svg</td>
<td>ImageMagick</td>
</tr>
</tbody>
</table>
<h3 id="3">3. 多语言绑定</h3>
<table>
<thead>
<tr>
<th>语言</th>
<th>包名</th>
<th>绑定技术</th>
</tr>
</thead>
<tbody>
<tr>
<td>Rust</td>
<td>liteparse</td>
<td>原生</td>
</tr>
<tr>
<td>Python</td>
<td>pip install liteparse</td>
<td>PyO3</td>
</tr>
<tr>
<td>Node.js</td>
<td>npm i @llamaindex/liteparse</td>
<td>napi-rs</td>
</tr>
<tr>
<td>Browser</td>
<td>npm i @llamaindex/liteparse-wasm</td>
<td>wasm-bindgen</td>
</tr>
</tbody>
</table>
<p>所有安装方式共享同一个 <code>lit</code> CLI。</p>
<h3 id="4-cli">4. 关键 CLI 命令</h3>
<pre><code># 基础解析
lit parse document.pdf

# JSON 格式 + 指定页
lit parse doc.pdf --format json --target-pages &quot;1-5,10&quot; -o output.json

# 批量解析
lit batch-parse ./input ./output

# 生成截图（供 LLM 视觉分析）
lit screenshot doc.pdf -o ./screenshots --dpi 300

# 指定 OCR 服务器
lit parse doc.pdf --ocr-server-url http://localhost:8000/ocr

# 管道输入
curl -sL https://example.com/report.pdf | lit parse -
</code></pre>
<h3 id="5-ocr">5. OCR 灵活架构</h3>
<p>三种模式可选：</p>
<ol>
<li><strong>Tesseract 内置</strong>：零配置开箱即用，适合大多数场景</li>
<li><strong>HTTP OCR 服务器</strong>：接入 EasyOCR/PaddleOCR，更高中文识别精度</li>
<li><strong>自定义 OCR</strong>：实现简单 API 规范即可（POST /ocr，接收 file + language，返回 text + bbox + confidence）</li>
</ol>
<h2 id="_3">关键概念</h2>
<ul>
<li><strong>PDFium</strong>：Google Chrome 内置的 PDF 渲染引擎，C 库，高性能文本提取</li>
<li><strong>Grid Projection</strong>：空间布局重建算法，将提取的文本元素按位置映射到网格，保留阅读顺序</li>
<li><strong>Bounding Box</strong>：每个文本元素的精确坐标 (x1, y1, x2, y2)，支持空间定位</li>
<li><strong>napi-rs / PyO3 / wasm-bindgen</strong>：Rust FFI 框架，分别用于 Node.js / Python / 浏览器绑定</li>
<li><strong>Selective OCR</strong>：仅对 PDFium 无法提取文本的区域执行 OCR，减少处理时间</li>
</ul>
<h2 id="_4">能力边界</h2>
<p><strong>适用场景</strong>：<br />
- 文本型 PDF 的快速结构化提取<br />
- 需要本地/离线运行的文档处理管道<br />
- 需要多语言（Python/Node/WASM）集成的项目<br />
- LLM Agent 的文档预处理（截图 + 文本双通道）</p>
<p><strong>局限性</strong>：<br />
- 复杂表格、多栏布局、手写体效果有限（官方推荐用付费 LlamaParse）<br />
- 扫描件依赖 OCR 质量，Tesseract 中文效果不如 PaddleOCR<br />
- Office 文档需额外安装 LibreOffice（服务器部署增加依赖）<br />
- 无云端模式，大文件处理受本地资源限制</p>
<h2 id="_5">最佳实践</h2>
<ol>
<li><strong>中文文档</strong>：外接 PaddleOCR HTTP 服务器替代 Tesseract，识别精度显著提升</li>
<li><strong>Agent 集成</strong>：利用 screenshot 功能生成视觉输入，text + image 双通道喂给 LLM</li>
<li><strong>批量处理</strong>：<code>batch-parse</code> 支持目录递归，配合 <code>--num-workers</code> 并行加速</li>
<li><strong>离线环境</strong>：预下载 <code>.traineddata</code> 文件，设置 <code>TESSDATA_PREFIX</code> 环境变量</li>
<li><strong>Agent Skill</strong>：<code>npx skills add run-llama/llamaparse-agent-skills --skill liteparse</code> 可直接作为 AI Agent 技能使用</li>
</ol>
<h2 id="_6">项目数据</h2>
<table>
<thead>
<tr>
<th>指标</th>
<th>值</th>
</tr>
</thead>
<tbody>
<tr>
<td>Star</td>
<td>6,433</td>
</tr>
<tr>
<td>Fork</td>
<td>411</td>
</tr>
<tr>
<td>语言</td>
<td>Rust</td>
</tr>
<tr>
<td>许可</td>
<td>Apache 2.0</td>
</tr>
<tr>
<td>创建时间</td>
<td>2026-02-09</td>
</tr>
<tr>
<td>最近更新</td>
<td>2026-05-28</td>
</tr>
<tr>
<td>绑定语言</td>
<td>Rust/Python/Node.js/WASM</td>
</tr>
</tbody>
</table>
<h2 id="_7">🔍 借鉴学习进化点（必填）</h2>
<table>
<thead>
<tr>
<th>#</th>
<th>点</th>
<th>原文要点</th>
<th>对当前系统的借鉴</th>
<th>行动计划</th>
</tr>
</thead>
<tbody>
<tr>
<td>1</td>
<td>Rust 核心 + 多语言绑定</td>
<td>用 PyO3/napi-rs/wasm-bindgen 一次实现多端分发</td>
<td>Hermes 的 Skill 系统可借鉴此模式，核心逻辑 Rust 化提升性能</td>
<td>无立即行动，但未来如需高性能解析可参考此架构</td>
</tr>
<tr>
<td>2</td>
<td>OCR 可插拔架构</td>
<td>Tesseract 内置 + HTTP OCR 服务器 + 自定义 API 规范</td>
<td>与 Hermes image-gen 降级链模式类似，可提炼为通用"可插拔能力层"模式</td>
<td>验证 current skills 中类似模式是否已文档化</td>
</tr>
<tr>
<td>3</td>
<td>Agent Skill 集成</td>
<td>提供 SKILL.md 供 AI Agent 直接调用</td>
<td>直接将 liteparse 集成为 Hermes Skill，用于本地 PDF/文档解析任务</td>
<td>pip install liteparse 后创建 liteparse skill</td>
</tr>
<tr>
<td>4</td>
<td>Screenshot + Text 双通道</td>
<td>为 LLM Agent 同时生成文本和截图</td>
<td>Hermes 处理文档任务时可采用此双通道策略，text 提取 + vision 分析互补</td>
<td>在 ocr-and-documents skill 中加入双通道建议</td>
</tr>
<tr>
<td>5</td>
<td>Grid Projection 空间布局</td>
<td>将文本元素映射到网格保持阅读顺序</td>
<td>笔记系统处理 PDF 内容时可利用 bounding box 信息重建文档结构</td>
<td>暂无直接行动，笔记系统当前不需要</td>
</tr>
</tbody>
</table>
<h2 id="rag-wiki">🔗 RAG / Wiki 联动（必填）</h2>
<h3 id="_8">当前状态</h3>
<ul>
<li>Wiki KB 已有文档处理相关 Skill（ocr-and-documents）</li>
<li>RAG 索引已建立，可检索笔记</li>
<li>Memory 记录了搜索降级链模式</li>
</ul>
<h3 id="_9">改进方案</h3>
<ol>
<li>liteparse 可作为 ocr-and-documents skill 的底层引擎替代 pymupdf/marker-pdf</li>
<li>中文 OCR 场景下，liteparse + PaddleOCR 服务器组合优于 Tesseract</li>
</ol>
<h3 id="_10">已执行操作</h3>
<ul>
<li>✅ 笔记已写入 ~/uclaw/bookmarks/notes/</li>
<li>⏳ 待评估是否安装 liteparse 并创建专用 Skill</li>
</ul>
<h2 id="_11">相关笔记</h2>
<!-- 待关联：新笔记暂无关联 -->
</body>
</html>