<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>cloudflare-mesh-tutorial</title>

<meta property="og:title" content="cloudflare-mesh-tutorial" />
<meta property="og:type" content="article" />
<meta property="og:description" content="笔记 | security" />
<meta property="og:image" content="https://hpage.top/homepage/covers/2026-04-17-cloudflare-mesh-tutorial.png" />

<style>
@import url('https://fonts.googleapis.com/css2?family=Noto+Sans+SC:wght@400..700&family=Noto+Serif+SC:wght@400..700&family=Space+Grotesk:wght@400..700&family=JetBrains+Mono:wght@400..500&family=IBM+Plex+Sans:wght@400..600&display=swap');

* { box-sizing: border-box; }

body {
  font-family: 'Fira Code', 'JetBrains Mono', monospace;
  max-width: 840px;
  margin: 0 auto;
  padding: 32px 24px;
  line-height: 1.85;
  font-weight: 450;
  color: #1f2937;
  background: #f3f4f6;
}

h1 {
  font-family: 'JetBrains Mono', 'Space Mono', monospace;
  font-size: 1.75em;
  font-weight: 700;
  color: #111827;
  border-bottom: none;
  padding-bottom: 0;
  margin-top: 0;
  margin-bottom: 0.6em;
}

h2 {
  font-family: 'JetBrains Mono', 'Space Mono', monospace;
  font-size: 1.35em;
  font-weight: 600;
  color: #111827;
  margin-top: 1.8em;
  margin-bottom: 0.5em;
  border-bottom: 1px solid #e5e7eb;
  padding-bottom: 6px;
}

h3 {
  font-family: 'JetBrains Mono', 'Space Mono', monospace;
  font-size: 1.12em;
  font-weight: 600;
  color: #111827cc;
  margin-top: 1.4em;
}

h4 {
  font-size: 1em;
  font-weight: 600;
  color: #111827aa;
}

a {
  color: #dc2626;
  text-decoration: none;
  border-bottom: 1px solid #dc262630;
  transition: border-color 0.2s;
}

a:hover {
  border-bottom-color: #dc2626;
}

code {
  background: #fef2f2;
  color: #dc2626;
  padding: 2px 7px;
  border-radius: 4px;
  font-size: 0.88em;
  font-family: 'JetBrains Mono', 'SF Mono', 'Fira Code', monospace;
}

pre {
  background: #111827;
  color: #f9fafb;
  border: none;
  padding: 20px 24px;
  border-radius: 8px;
  overflow-x: auto;
  line-height: 1.6;
  font-size: 0.88em;
  margin: 1.2em 0;
  box-shadow: 0 2px 8px rgba(0,0,0,0.08);
}

pre code {
  background: none;
  color: inherit;
  padding: 0;
  font-size: inherit;
}

blockquote {
  border-left: 4px solid #dc2626;
  margin: 1.2em 0;
  padding: 12px 20px;
  color: #1f2937cc;
  background: #fef2f2;
  border-radius: 0 6px 6px 0;
}

/* ═══ table：彻底禁用 ═══ */
table, th, td { display: none !important; }

img {
  max-width: 100%;
  border-radius: 8px;
  margin: 8px 0;
  box-shadow: 0 1px 4px rgba(0,0,0,0.06);
}

hr {
  border: none;
  height: 1px;
  background: #e5e7eb;
  margin: 2.5em 0;
}

ul, ol {
  padding-left: 1.6em;
}

li { margin-bottom: 4px; }

strong { color: #111827; }


        h1::before { content: '🛡 '; }
        pre { border-left: 3px solid #dc2626; }
        

/* 响应式 */
@media (max-width: 640px) {
  body { padding: 20px 16px; }
  h1 { font-size: 1.45em; }
  h2 { font-size: 1.2em; }
  pre { padding: 14px 16px; font-size: 0.82em; }
}

/* 打印优化 */
@media print {
  body { background: white; color: black; max-width: 100%; }
  pre { border: 1px solid #ddd; box-shadow: none; }
  a { color: #333; border-bottom: none; }
}
</style>
</head>
<body>
<h1 id="cloudflare-mesh-ip">Cloudflare Mesh 保姆级教程：不用公网 IP，把设备和服务器连成私有网络</h1>
<blockquote>
<p>来源：<a href="https://x.com/xiangxiang103/status/2044973283423277548">@xiangxiang103 雨哥向前冲</a><br />
日期：2026-04-17 | 浏览：4.8万 | 收藏：277 | 点赞：118</p>
</blockquote>
<h2 id="_1">核心概念</h2>
<p>Cloudflare Mesh 可以把电脑、手机、服务器、Cloudflare Workers 连成跨地域"局域网"。无需公网 IP，设备间通过私有 IP（默认 100.96.0.0/12）互访。</p>
<p><strong>四大组件：</strong></p>
<table>
<thead>
<tr>
<th>组件</th>
<th>作用</th>
</tr>
</thead>
<tbody>
<tr>
<td>Client Device</td>
<td>手机/电脑装 Cloudflare One 客户端，登录获 Mesh IP</td>
</tr>
<tr>
<td>Mesh Node</td>
<td>服务端节点（原 WARP Connector），装服务器上入网或做网关</td>
</tr>
<tr>
<td>Mesh IP</td>
<td>组网后分配的私网地址，默认 100.96.0.0/12 段</td>
</tr>
<tr>
<td>Workers VPC</td>
<td>新功能，让边缘 Workers 连入私有网络，跨网调内网接口</td>
</tr>
</tbody>
</table>
<p><strong>免费额度：</strong> 50 节点 + 50 用户，个人开发者够用。</p>
<h2 id="_2">实操步骤</h2>
<h3 id="1-node">1. 创建 Node</h3>
<p>CF Dashboard → Networking → Mesh → Add a node，跟着向导走。</p>
<h3 id="2">2. 服务器入网</h3>
<pre><code class="language-bash"># 安装 cloudflare-warp（注意包名）
sudo warp-cli connector new &lt;TOKEN&gt;
sudo warp-cli connect
</code></pre>
<p>Dashboard 显示 Online 即成功。</p>
<h3 id="3">3. 本地电脑入网</h3>
<p>装 WARP 客户端 → 偏好设置 → Zero Trust security → 填 team name 登录 → Connected 即可。用 Mesh IP ping/SSH 测试连通性。</p>
<h3 id="4-workers-vpc">4. Workers VPC 绑定（进阶）</h3>
<pre><code class="language-jsonc">// wrangler.jsonc
{
  &quot;vpc_networks&quot;: [{
    &quot;binding&quot;: &quot;MESH&quot;,
    &quot;network_id&quot;: &quot;cf1:network&quot;,  // 固定写法
    &quot;remote&quot;: true
  }]
}
</code></pre>
<pre><code class="language-js">// 直接 fetch 内网接口
const NODE_IP = &quot;100.96.1.50&quot;;
export default {
  async fetch(request, env) {
    return await env.MESH.fetch(`http://${NODE_IP}:8080/api/data`);
  }
}
</code></pre>
<h2 id="_3">踩坑要点</h2>
<ol>
<li><strong>Split Tunnel 拉黑</strong>：默认排除 100.64.0.0/10 包含 Mesh 段 100.96.0.0/12，需手动放行</li>
<li><strong>只能用 IP</strong>：Workers 调局域网暂不支持域名，Mesh DNS 后续更新</li>
<li><strong>无 Docker 支持</strong>：目前靠客户端/Connector 接入，官方 Docker 镜像未发布</li>
<li><strong>Ping 不通≠断网</strong>：服务器可能禁 ICMP，用 SSH/curl 确认</li>
</ol>
<h2 id="_4">我的分析与思考</h2>
<p><strong>与现有架构对比：</strong><br />
- 我们目前用 Tailscale 做节点连接（OpenClaw node-connect）<br />
- Cloudflare Mesh 本质是同赛道方案，基于 CF 全球边缘网络<br />
- 优势：免费额度更大（Tailscale 免费仅 3 用户 100 设备）、Workers 生态集成<br />
- 劣势：成熟度不如 Tailscale、暂无 DNS 支持、Docker 缺失</p>
<p><strong>Jetson/边缘场景价值：</strong><br />
- 不需公网 IP 就能组网，适合边缘设备远程管理<br />
- 但 Jetson Orin Nano (arm64) 需确认 cloudflare-warp 是否支持</p>
<p><strong>趋势预判：</strong><br />
- Cloudflare 在网络层持续发力（Tunnel → Mesh → VPC），趋势是让开发者完全不需要传统网络知识<br />
- Workers VPC 让边缘计算和私有基础设施打通，这对 AI Agent 部署模式有深远影响<br />
- 值得持续关注 Workers VPC 正式版和 Mesh DNS 功能</p>
<p><strong>可行动项：</strong><br />
- [ ] 测试 Jetson 上安装 cloudflare-warp (arm64)<br />
- [ ] 对比 Tailscale vs CF Mesh 延迟和稳定性<br />
- [ ] 探索 Workers VPC 调内网 AI 接口的可行性</p>
<h2 id="_5">相关笔记</h2>
<ul>
<li><a href="2026-04-12-cloudflare-email-routing.md">2026-04-12-cloudflare-email-routing</a> - Cloudflare Email Routing 配置</li>
<li><a href="2026-04-14-Cloudflare-EmDash-AI-Agent-CMS.md">2026-04-14-Cloudflare-EmDash-AI-Agent-CMS</a> - Cloudflare EmDash AI Agent CMS</li>
</ul>
</body>
</html>