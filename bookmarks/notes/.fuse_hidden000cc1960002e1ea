<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>从零构建托管式AI-Agent-服务端循环会话与工具调用</title>

<meta property="og:title" content="从零构建托管式AI-Agent-服务端循环会话与工具调用" />
<meta property="og:type" content="article" />
<meta property="og:description" content="笔记 | academic" />
<meta property="og:image" content="https://hpage.top/homepage/covers/2026-08-11-%E4%BB%8E%E9%9B%B6%E6%9E%84%E5%BB%BA%E6%89%98%E7%AE%A1%E5%BC%8FAI-Agent-%E6%9C%8D%E5%8A%A1%E7%AB%AF%E5%BE%AA%E7%8E%AF%E4%BC%9A%E8%AF%9D%E4%B8%8E%E5%B7%A5%E5%85%B7%E8%B0%83%E7%94%A8.png" />

<style>
@import url('https://fonts.googleapis.com/css2?family=Noto+Sans+SC:wght@400..700&family=Noto+Serif+SC:wght@400..700&family=Space+Grotesk:wght@400..700&family=JetBrains+Mono:wght@400..500&family=IBM+Plex+Sans:wght@400..600&display=swap');

* { box-sizing: border-box; }

body {
  font-family: 'Noto Serif SC', 'Source Serif 4', Georgia, serif;
  max-width: 840px;
  margin: 0 auto;
  padding: 32px 24px;
  line-height: 1.85;
  font-weight: 450;
  color: #2c2c2c;
  background: #faf8f5;
}

h1 {
  font-family: 'Noto Serif SC', 'Playfair Display', Georgia, serif;
  font-size: 1.75em;
  font-weight: 700;
  color: #8b1a1a;
  border-bottom: none;
  padding-bottom: 0;
  margin-top: 0;
  margin-bottom: 0.6em;
}

h2 {
  font-family: 'Noto Serif SC', 'Playfair Display', Georgia, serif;
  font-size: 1.35em;
  font-weight: 600;
  color: #8b1a1a;
  margin-top: 1.8em;
  margin-bottom: 0.5em;
  border-bottom: 1px solid #e8e0d4;
  padding-bottom: 6px;
}

h3 {
  font-family: 'Noto Serif SC', 'Playfair Display', Georgia, serif;
  font-size: 1.12em;
  font-weight: 600;
  color: #8b1a1acc;
  margin-top: 1.4em;
}

h4 {
  font-size: 1em;
  font-weight: 600;
  color: #8b1a1aaa;
}

a {
  color: #8b4513;
  text-decoration: none;
  border-bottom: 1px solid #8b451330;
  transition: border-color 0.2s;
}

a:hover {
  border-bottom-color: #8b4513;
}

code {
  background: #f7f5f0;
  color: #9a5b13;
  padding: 2px 7px;
  border-radius: 4px;
  font-size: 0.88em;
  font-family: 'JetBrains Mono', 'SF Mono', 'Fira Code', monospace;
}

pre {
  background: #3c3226;
  color: #f5f0e8;
  border: none;
  padding: 20px 24px;
  border-radius: 8px;
  overflow-x: auto;
  line-height: 1.6;
  font-size: 0.88em;
  margin: 1.2em 0;
  box-shadow: 0 2px 8px rgba(0,0,0,0.08);
}

pre code {
  background: none;
  color: inherit;
  padding: 0;
  font-size: inherit;
}

blockquote {
  border-left: 4px solid #a0522d;
  margin: 1.2em 0;
  padding: 12px 20px;
  color: #2c2c2ccc;
  background: #f5f0e8;
  border-radius: 0 6px 6px 0;
}

/* ═══ table：彻底禁用 ═══ */
table, th, td { display: none !important; }

img {
  max-width: 100%;
  border-radius: 8px;
  margin: 8px 0;
  box-shadow: 0 1px 4px rgba(0,0,0,0.06);
}

hr {
  border: none;
  height: 1px;
  background: #e8e0d4;
  margin: 2.5em 0;
}

ul, ol {
  padding-left: 1.6em;
}

li { margin-bottom: 4px; }

strong { color: #8b1a1a; }


        h1 { font-style: italic; }
        h1::before { content: '§ '; color: #a0522d40; font-size: 0.7em; vertical-align: middle; }
        

/* 响应式 */
@media (max-width: 640px) {
  body { padding: 20px 16px; }
  h1 { font-size: 1.45em; }
  h2 { font-size: 1.2em; }
  pre { padding: 14px 16px; font-size: 0.82em; }
}

/* 打印优化 */
@media print {
  body { background: white; color: black; max-width: 100%; }
  pre { border: 1px solid #ddd; box-shadow: none; }
  a { color: #333; border-bottom: none; }
}
</style>
</head>
<body>
<h1 id="ai-agent-anthropic-managed-agents">从零构建第一个托管式 AI Agent — 服务端循环、会话与工具调用（Anthropic Managed Agents 课件）</h1>
<blockquote>
<p><strong>来源</strong>：<a href="https://my.feishu.cn/docx/Jy8XdmON5oFXnHxTUqQcv5dBnQb">飞书文档</a>（基于原视频《Ship your first Managed Agent》中文课件）<br />
<strong>视频时长</strong>：36:49<br />
<strong>核心产品</strong>：Claude Managed Agents（Anthropic 托管式 Agent）<br />
<strong>标签</strong>：Managed Agent、Agent Loop、Session、Event Stream、SRE、Context Engineering</p>
</blockquote>
<hr />
<h2 id="_1">一句话定义</h2>
<blockquote>
<p><strong>托管式 Agent = 你提供任务+工具+领域逻辑，Anthropic 托管 Agent loop+沙箱+会话持久化+鉴权+扩缩容。</strong> 三件套（Agent=大脑、Environment=双手、Session=连接）组成最小可持续任务。核心设计：Agent loop 在服务端运行，工具按需在沙箱中启动，通过事件流双向通信。</p>
</blockquote>
<hr />
<h2 id="_2">完整原文核心内容</h2>
<h3 id="messages-api-agent">一、三层演进：从 Messages API 到托管式 Agent</h3>
<table>
<thead>
<tr>
<th>层级</th>
<th>开发者负责</th>
<th>平台托管</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>Messages API</strong></td>
<td>几乎全部：上下文管理、Agent loop、工具执行、会话恢复、鉴权、可观测性</td>
<td>仅模型输入输出</td>
</tr>
<tr>
<td><strong>Agent SDK</strong></td>
<td>自定义工具、领域逻辑、hosting/scaling、安全容器</td>
<td>Claude Code 能力封装</td>
</tr>
<tr>
<td><strong>Managed Agents</strong></td>
<td>task + agent config + custom tool logic（含 MCP/Skills）</td>
<td>harness + context management + tool runtime + sandbox + session persistence + auth vault + hosting/scaling/observability</td>
</tr>
</tbody>
</table>
<blockquote>
<p><strong>"context anxiety"</strong>：团队曾针对某模型"过早结束任务"的行为加入缓解逻辑，模型更新后该行为消失，补丁失去价值。→ <strong>harness 必须跟着模型行为演进，不是一次性写完。</strong></p>
</blockquote>
<hr />
<h3 id="_3">二、三件套与"脑—手"解耦</h3>
<table>
<thead>
<tr>
<th>原语</th>
<th>回答的问题</th>
<th>比喻</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>Agent</strong></td>
<td>模型、系统提示词、工具和能力是什么？</td>
<td>大脑</td>
</tr>
<tr>
<td><strong>Environment</strong></td>
<td>工具在哪里执行？容器/网络/计算资源？</td>
<td>双手</td>
</tr>
<tr>
<td><strong>Session</strong></td>
<td>哪个 Agent 在哪个 Environment 中为哪个任务工作？</td>
<td>连接与事件通道</td>
</tr>
</tbody>
</table>
<p><strong>"脑—手"解耦的核心价值</strong>：<br />
- 旧模型：每个会话一个容器，Agent loop 与工具执行绑定<br />
- 新模型：Agent loop 由 Anthropic 管理，工具按需在 sandbox 中启动<br />
- 收益：凭证隔离更清楚、工具容器不必和循环绑死、容器按需启动降低首 token 延迟<br />
- TTFT P95 降低 &gt;90%（视频归因于其架构的观测值，非通用保证）</p>
<hr />
<h3 id="sre">三、SRE 事故响应实战演示</h3>
<p><strong>事故场景</strong>：INCIDENT-2277<br />
- checkout 服务 P99 latency ~3638ms（约 10 倍 baseline）<br />
- error rate 21.9%<br />
- DB pool 100%</p>
<p><strong>工具链</strong>：</p>
<table>
<thead>
<tr>
<th>工具</th>
<th>输入</th>
<th>用途</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>get_metrics</code></td>
<td>服务名+指标名</td>
<td>获取指标数据</td>
</tr>
<tr>
<td><code>get_recent_deploys</code></td>
<td>无复杂参数</td>
<td>获取最近部署记录</td>
</tr>
<tr>
<td><code>get_diff</code></td>
<td>commit 标识</td>
<td>获取代码变更</td>
</tr>
</tbody>
</table>
<p><strong>调查过程</strong>：</p>
<pre><code>用户: &quot;debug my incident for me&quot;
→ sandbox/bash (分析日志)
→ get_recent_deploys (找最近变更)
→ get_metrics (确认指标异常)
→ get_diff (查看代码变更)
→ 结论: database pool exhaustion（数据库连接池耗尽）
  关联到 Alice 的 order summary builder 重构查询
  建议: 回滚 + 确认连接池告警 + 增加回归测试
</code></pre>
<p><strong>Agent 创建代码</strong>（按视频画面）：</p>
<pre><code class="language-python">agent = client.beta.agents.create(
    name=&quot;SRE Agent&quot;,
    model=&quot;claude-opus-4-7&quot;,
    system=SYSTEM,
    tools=TOOLS,
)
</code></pre>
<hr />
<h3 id="session">四、Session + 事件流（核心协议）</h3>
<h4 id="_4">事件类型</h4>
<table>
<thead>
<tr>
<th>方向</th>
<th>事件</th>
<th>语义</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>发送</strong></td>
<td><code>user.message</code></td>
<td>用户消息</td>
</tr>
<tr>
<td><strong>发送</strong></td>
<td><code>user.custom_tool_result</code></td>
<td>自定义工具调用的结果</td>
</tr>
<tr>
<td><strong>发送</strong></td>
<td><code>user.tool_confirmation</code></td>
<td>对需权限确认的工具批准/拒绝</td>
</tr>
<tr>
<td><strong>发送</strong></td>
<td><code>user.interrupt</code></td>
<td>用户发起中断</td>
</tr>
<tr>
<td><strong>接收</strong></td>
<td><code>agent.message</code></td>
<td>Agent 的回复</td>
</tr>
<tr>
<td><strong>接收</strong></td>
<td><code>agent.tool_use</code></td>
<td>调用内置工具</td>
</tr>
<tr>
<td><strong>接收</strong></td>
<td><code>agent.custom_tool_use</code></td>
<td>调用你提供的工具</td>
</tr>
<tr>
<td><strong>接收</strong></td>
<td><code>agent.mcp_tool_use</code></td>
<td>调用 MCP 工具</td>
</tr>
<tr>
<td><strong>接收</strong></td>
<td><code>session.status_idle</code></td>
<td>循环暂停，携带停止原因</td>
</tr>
<tr>
<td><strong>接收</strong></td>
<td><code>session.error</code></td>
<td>发生失败</td>
</tr>
</tbody>
</table>
<h4 id="_5">事件流代码（按视频画面重排）</h4>
<pre><code class="language-python">with client.beta.sessions.events.stream(session_id) as stream:
    client.beta.sessions.events.send(session_id, events=[
        {&quot;type&quot;: &quot;user.message&quot;,
         &quot;content&quot;: [{&quot;type&quot;: &quot;text&quot;, &quot;text&quot;: user_text}]}
    ])
    for ev in stream:
        if ev.type == &quot;agent.custom_tool_use&quot;:
            result = handle_tool(ev.name, ev.input)
            client.beta.sessions.events.send(
                session_id,
                events=[{
                    &quot;type&quot;: &quot;user.custom_tool_result&quot;,
                    &quot;custom_tool_use_id&quot;: ev.id,
                    &quot;content&quot;: [{&quot;type&quot;: &quot;text&quot;, &quot;text&quot;: result}]
                }],
            )
        yield ev
</code></pre>
<blockquote>
<p>关键方向：云端 Agent 发出工具事件 → 本地 handle_tool 读取 fixture → 结果作为用户侧工具结果发回 → 循环继续。</p>
</blockquote>
<h4 id="4">会话状态机（4 种状态）</h4>
<table>
<thead>
<tr>
<th>状态</th>
<th>说明</th>
<th>转换</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>idle</strong></td>
<td>等待输入</td>
<td>← running（结束/需要动作）</td>
</tr>
<tr>
<td><strong>running</strong></td>
<td>Agent 正在执行循环</td>
<td>→ idle / rescheduling / terminated</td>
</tr>
<tr>
<td><strong>rescheduling</strong></td>
<td>瞬态错误，自动重试</td>
<td>→ running（auto-retry）</td>
</tr>
<tr>
<td><strong>terminated</strong></td>
<td>不可恢复错误</td>
<td>终态</td>
</tr>
</tbody>
</table>
<hr />
<h3 id="beyond-the-basics">五、扩展能力（Beyond the Basics）</h3>
<table>
<thead>
<tr>
<th>能力</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>Subagents</strong></td>
<td>orchestrator 把任务分给子 Agent，各自上下文窗口，并行化+上下文隔离</td>
</tr>
<tr>
<td><strong>Memory / dreaming</strong></td>
<td>从日志、用户纠正和偏好中判断哪些值得保留</td>
</tr>
<tr>
<td><strong>Outcomes</strong></td>
<td>用 rubric（评估量规）定义期望产物</td>
</tr>
<tr>
<td><strong>Vaults</strong></td>
<td>凭证独立端点加密访问，按用户管理</td>
</tr>
<tr>
<td><strong>Webhooks</strong></td>
<td>外部事件触发或恢复 Session</td>
</tr>
<tr>
<td><strong>Permission policies</strong></td>
<td>工具确认和权限控制</td>
</tr>
<tr>
<td><strong>MCP servers</strong></td>
<td>远程工具服务器</td>
</tr>
<tr>
<td><strong>Interrupt</strong></td>
<td>运行中断机制</td>
</tr>
<tr>
<td><strong>Console agent builder</strong></td>
<td>交互式配置和可观测性</td>
</tr>
</tbody>
</table>
<blockquote>
<p>⚠️ 多数扩展带 Beta 标识，不能当成稳定接口。</p>
</blockquote>
<hr />
<h3 id="_6">六、完整心智模型（一张可迁移的图）</h3>
<pre><code>用户消息
   ↓
Session（绑定 Agent + Environment + 资源）
   ↓ 事件流
服务端 Agent loop（选择下一步）
   ↓ agent.custom_tool_use
工具执行端（本地脚本 / 自有容器 / 生产客户端）
   ↓ user.custom_tool_result
服务端 Agent loop 继续推理并产出 agent.message
   ↓
前端呈现 + Session 日志 + 可观测性
</code></pre>
<table>
<thead>
<tr>
<th>层</th>
<th>负责</th>
<th>证据</th>
</tr>
</thead>
<tbody>
<tr>
<td>Agent 定义</td>
<td>模型、系统提示词、工具和能力</td>
<td>SRE Agent 创建代码</td>
</tr>
<tr>
<td>Environment</td>
<td>容器、网络和工具执行空间</td>
<td>config 与 sandbox 说明</td>
</tr>
<tr>
<td>资源/上下文</td>
<td>日志、指标、部署、diff 等证据</td>
<td>app.log 与本地 JSON</td>
</tr>
<tr>
<td>Session</td>
<td>绑定资源并维持任务生命周期</td>
<td>Session 创建与刷新恢复</td>
</tr>
<tr>
<td>事件流</td>
<td>用户消息、工具调用、结果、状态</td>
<td>stream_reply 与事件幻灯片</td>
</tr>
<tr>
<td>工具执行端</td>
<td>真正查询数据或执行动作</td>
<td>handle_tool、Datadog 替换方向</td>
</tr>
<tr>
<td>控制与观测</td>
<td>日志、状态、删除、确认和恢复</td>
<td>控制台、状态机、删除按钮</td>
</tr>
</tbody>
</table>
<hr />
<h3 id="demo">七、实践路线（从 Demo 到生产）</h3>
<ol>
<li><strong>复现输入</strong> — 本地 fixture 启动 Streamlit，验证页面和数据格式</li>
<li><strong>定义边界</strong> — 最小 Agent system prompt + 工具 schema + Environment 网络 allow list + 日志挂载规则</li>
<li><strong>接通会话</strong> — 创建 Session，发送 user.message，实现事件流回传</li>
<li><strong>做证据闭环</strong> — 每次工具调用+输入+输出+耗时+错误+结论写入可查询日志</li>
<li><strong>验证生命周期</strong> — 测试 idle/running/rescheduling/terminated + 刷新恢复 + 删除 + 人工中断</li>
<li><strong>替换数据源</strong> — 用生产客户端替代 JSON fixture，保持工具契约稳定</li>
<li><strong>加控制面</strong> — 写操作增加工具确认/审批/审计/回滚/webhook 鉴权</li>
<li><strong>最后扩展</strong> — 按一个目标逐次加入 Subagents/Memory/Outcomes/Vaults</li>
</ol>
<hr />
<h2 id="_7">结合最新研究与技术变化</h2>
<h3 id="1-hermes-agent">1. 与 Hermes Agent 架构的直接映射</h3>
<table>
<thead>
<tr>
<th>Managed Agents 概念</th>
<th>Hermes 对应</th>
</tr>
</thead>
<tbody>
<tr>
<td>Agent 定义（模型+系统提示词+工具）</td>
<td>config.yaml + Skill 系统</td>
</tr>
<tr>
<td>Environment（容器+网络+执行空间）</td>
<td>Hermes Gateway + terminal/execute_code 沙箱</td>
</tr>
<tr>
<td>Session（绑定 Agent+Env+资源）</td>
<td>Hermes session（会话持久化）</td>
</tr>
<tr>
<td>事件流（user.message ↔ agent.custom_tool_use）</td>
<td>Hermes tool_call ↔ tool_result 循环</td>
</tr>
<tr>
<td>Context Engineering（上下文工程）</td>
<td>Hermes MEMORY.md + Skill 按需加载</td>
</tr>
<tr>
<td>Session 状态机（idle/running/rescheduling/terminated）</td>
<td>Hermes 会话生命周期</td>
</tr>
<tr>
<td>Subagents</td>
<td>delegate_task</td>
</tr>
<tr>
<td>Memory / dreaming</td>
<td>MEMORY.md + session_search + cron memory consolidation</td>
</tr>
<tr>
<td>Vaults（凭证保险库）</td>
<td>config.yaml 环境变量注入</td>
</tr>
</tbody>
</table>
<h3 id="2-physiclaw-turn-loop">2. 与 PhysiClaw Turn Loop 的关联</h3>
<p>PhysiClaw 的 <code>loop.py</code>（7 步管线：model→tool_calls→dispatch→results→model）与本课件描述的 Managed Agent loop 是同构的。区别在于：<br />
- Managed Agents 的 loop 在服务端运行，工具在沙箱中执行<br />
- PhysiClaw 的 loop 在本地运行，工具是物理的（相机/触控笔）<br />
- Hermes 的 loop 也在本地运行，工具是数字的（API/文件/终端）</p>
<h3 id="3-coding-agent-harness">3. 与 Coding Agent Harness 解剖学的关联</h3>
<p>Mastra 的 27 分钟深度分析也描述了同样的 Agent loop 模式——model→tool_calls→tool_results→model。三个独立来源（Anthropic/Mastra/PhysiClaw）验证了这是 2026 年 Agent 架构的共识。</p>
<h3 id="4-context-engineering">4. 与 Context Engineering 的关联</h3>
<p>本课件明确提出 <strong>"context engineering（上下文工程）"</strong> 是 Agent 质量的主要杠杆——"把更多正确上下文交给它"比不断加长系统提示词更重要。这与 Hermes 的 L1常驻/L2阶段触发/L3按需查询 三层加载策略完全一致。</p>
<hr />
<h2 id="_8">学习进化借鉴点</h2>
<table>
<thead>
<tr>
<th>洞察</th>
<th>可借鉴到 Hermes</th>
<th>优先级</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>Agent/Environment/Session 三件套</strong></td>
<td>架构梳理</td>
<td>高</td>
</tr>
<tr>
<td><strong>事件流比请求/响应更适合可恢复 Agent</strong></td>
<td>会话设计</td>
<td>高</td>
</tr>
<tr>
<td><strong>"脑—手"解耦降低 TTFT &gt;90%</strong></td>
<td>工具执行架构</td>
<td>高</td>
</tr>
<tr>
<td><strong>context anxiety 概念</strong></td>
<td>harness 必须跟模型演进</td>
<td>高</td>
</tr>
<tr>
<td><strong>状态机（idle/running/rescheduling/terminated）</strong></td>
<td>会话生命周期管理</td>
<td>高</td>
</tr>
<tr>
<td><strong>本地 fixture → 生产客户端迁移路径</strong></td>
<td>工具替换策略</td>
<td>中</td>
</tr>
<tr>
<td><strong>Outcomes rubric</strong></td>
<td>任务完成质量评估</td>
<td>中</td>
</tr>
<tr>
<td><strong>Vaults 凭证管理</strong></td>
<td>安全性提升</td>
<td>中</td>
</tr>
<tr>
<td><strong>webhook 作为控制面</strong></td>
<td>外部事件触发</td>
<td>中</td>
</tr>
</tbody>
</table>
<hr />
<h2 id="_9">趋势判断</h2>
<ol>
<li><strong>Agent loop 即服务（Loop-as-a-Service）</strong> — Anthropic 把 Agent loop 托管在服务端，开发者只需提供工具和领域逻辑</li>
<li><strong>事件流 &gt; 请求/响应</strong> — 长任务可恢复、可审计、可观测的需求推动从一次性请求转向持续事件流</li>
<li><strong>"脑—手"解耦成为标配</strong> — Agent loop 与工具执行分离，凭证隔离+按需启动+降低延迟</li>
<li><strong>Context Engineering 成为核心学科</strong> — 比系统提示词更重要的是"上传哪些文件、怎样组织、工具返回什么证据"</li>
<li><strong>托管式 Agent 降低生产门槛</strong> — 视频称 10-15x 更快到生产（非通用保证）</li>
<li><strong>状态机 + 持久化 + webhook = 可恢复 Agent</strong> — 容器崩溃后从 Session 日志继续</li>
<li><strong>Memory/Outcomes/Vaults 是下一层扩展</strong> — 跨会话记忆、质量量规、凭证安全</li>
</ol>
<hr />
<h2 id="_10">关联分析</h2>
<ul>
<li><strong>与 PhysiClaw</strong>：同构的 Turn Loop，但工具从数字变物理</li>
<li><strong>与 Coding Agent Harness（Mastra）</strong>：三个来源验证 Agent loop 共识</li>
<li><strong>与 Context Engineering 四大支柱</strong>：L1/L2/L3 分层加载 = context engineering 的具体实现</li>
<li><strong>与 Hermes 架构</strong>：几乎所有概念都能找到对应实现</li>
<li><strong>与 ADB 本地算力机</strong>：Managed Agent 的 Environment = AIPC 的沙箱</li>
<li><strong>与 CodexLoom Agent Team</strong>：Subagents 概念的两种实现（orchestrator 编排 vs 长期责任分配）</li>
</ul>
<hr />
<p><em>笔记创建：2026-08-11 · 来源：飞书文档（Anthropic Managed Agents 中文课件）</em></p>
</body>
</html>