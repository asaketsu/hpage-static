<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>CATIA知识工程深度研究</title>

<meta property="og:title" content="CATIA知识工程深度研究" />
<meta property="og:type" content="article" />
<meta property="og:description" content="笔记 | academic" />
<meta property="og:image" content="https://hpage.top/homepage/covers/academic.png" />

<style>
@import url('https://fonts.googleapis.com/css2?family=Noto+Sans+SC:wght@400..700&family=Noto+Serif+SC:wght@400..700&family=Space+Grotesk:wght@400..700&family=JetBrains+Mono:wght@400..500&family=IBM+Plex+Sans:wght@400..600&display=swap');

* { box-sizing: border-box; }

body {
  font-family: 'Noto Serif SC', 'Source Serif 4', Georgia, serif;
  max-width: 840px;
  margin: 0 auto;
  padding: 32px 24px;
  line-height: 1.85;
  font-weight: 450;
  color: #2c2c2c;
  background: #faf8f5;
}

h1 {
  font-family: 'Noto Serif SC', 'Playfair Display', Georgia, serif;
  font-size: 1.75em;
  font-weight: 700;
  color: #8b1a1a;
  border-bottom: none;
  padding-bottom: 0;
  margin-top: 0;
  margin-bottom: 0.6em;
}

h2 {
  font-family: 'Noto Serif SC', 'Playfair Display', Georgia, serif;
  font-size: 1.35em;
  font-weight: 600;
  color: #8b1a1a;
  margin-top: 1.8em;
  margin-bottom: 0.5em;
  border-bottom: 1px solid #e8e0d4;
  padding-bottom: 6px;
}

h3 {
  font-family: 'Noto Serif SC', 'Playfair Display', Georgia, serif;
  font-size: 1.12em;
  font-weight: 600;
  color: #8b1a1acc;
  margin-top: 1.4em;
}

h4 {
  font-size: 1em;
  font-weight: 600;
  color: #8b1a1aaa;
}

a {
  color: #8b4513;
  text-decoration: none;
  border-bottom: 1px solid #8b451330;
  transition: border-color 0.2s;
}

a:hover {
  border-bottom-color: #8b4513;
}

code {
  background: #f7f5f0;
  color: #9a5b13;
  padding: 2px 7px;
  border-radius: 4px;
  font-size: 0.88em;
  font-family: 'JetBrains Mono', 'SF Mono', 'Fira Code', monospace;
}

pre {
  background: #3c3226;
  color: #f5f0e8;
  border: none;
  padding: 20px 24px;
  border-radius: 8px;
  overflow-x: auto;
  line-height: 1.6;
  font-size: 0.88em;
  margin: 1.2em 0;
  box-shadow: 0 2px 8px rgba(0,0,0,0.08);
}

pre code {
  background: none;
  color: inherit;
  padding: 0;
  font-size: inherit;
}

blockquote {
  border-left: 4px solid #a0522d;
  margin: 1.2em 0;
  padding: 12px 20px;
  color: #2c2c2ccc;
  background: #f5f0e8;
  border-radius: 0 6px 6px 0;
}

/* ═══ table：彻底禁用 ═══ */
table, th, td { display: none !important; }

img {
  max-width: 100%;
  border-radius: 8px;
  margin: 8px 0;
  box-shadow: 0 1px 4px rgba(0,0,0,0.06);
}

hr {
  border: none;
  height: 1px;
  background: #e8e0d4;
  margin: 2.5em 0;
}

ul, ol {
  padding-left: 1.6em;
}

li { margin-bottom: 4px; }

strong { color: #8b1a1a; }


        h1 { font-style: italic; }
        h1::before { content: '§ '; color: #a0522d40; font-size: 0.7em; vertical-align: middle; }
        

/* 响应式 */
@media (max-width: 640px) {
  body { padding: 20px 16px; }
  h1 { font-size: 1.45em; }
  h2 { font-size: 1.2em; }
  pre { padding: 14px 16px; font-size: 0.82em; }
}

/* 打印优化 */
@media print {
  body { background: white; color: black; max-width: 100%; }
  pre { border: 1px solid #ddd; box-shadow: none; }
  a { color: #333; border-bottom: none; }
}
</style>
</head>
<body>
<h1 id="catia">CATIA 知识工程深度研究</h1>
<ul>
<li>📅 2026-05-22</li>
<li>⭐ 5/5 — CATIA Knowledgeware 是 CAD 知识自动化核心，AI 融合潜力巨大</li>
<li>🏷️ #CATIA #Knowledgeware #KBE #KWA #KWE #知识工程 #CAA #参数化设计 #AI-CAD</li>
<li>📰 Dassault Systèmes 官方文档 + 社区资源 + 领域知识整合</li>
<li>🔗 https://www.3ds.com/products/catia/ai-driven-generative-experiences</li>
</ul>
<h2 id="kbe-vs-knowledgeware">术语厘清：KBE vs Knowledgeware</h2>
<table>
<thead>
<tr>
<th>术语</th>
<th>全称</th>
<th>含义</th>
<th style="text-align: center;">性质</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>KBE</strong></td>
<td>Knowledge-<strong>Based</strong> Engineering</td>
<td>知识工程<strong>方法论</strong>（通用行业术语）</td>
<td style="text-align: center;">🌐 行业通用</td>
</tr>
<tr>
<td><strong>Knowledgeware</strong></td>
<td>CATIA Knowledgeware</td>
<td>Dassault Systèmes 的 CATIA 知识工程<strong>产品族名称</strong>（一个词）</td>
<td style="text-align: center;">🏷️ CATIA 产品</td>
</tr>
</tbody>
</table>
<blockquote>
<p><strong>写作规范</strong>：描述 CATIA 产品功能时用 "Knowledgeware"；描述行业方法论时用 "KBE"。两者不可混用。</p>
</blockquote>
<h2 id="_1">摘要</h2>
<p>CATIA <strong>Knowledgeware</strong> 是 Dassault Systèmes 在 CATIA V5/V6/3DEXPERIENCE 中内置的一套<strong>设计知识捕获、重用和自动化</strong>产品族。它遵循 KBE（Knowledge-Based Engineering）方法论，通过参数（Parameters）、公式（Formulas）、规则（Rules）、检查（Checks）、反应特征（Reactive Features）、设计表（Design Tables）、目录（Catalogs）、Power Copy 和 User Feature 等机制，将工程师的设计经验、标准和约束编码为可执行的工程知识，实现从"手工画图"到"知识驱动设计"的范式转变。</p>
<hr />
<h2 id="_2">一、知识工程产品线总览</h2>
<p>CATIA <strong>Knowledgeware</strong> 产品族由多个产品模块组成，是 Dassault 对 KBE 方法论的商业实现：</p>
<table>
<thead>
<tr>
<th>产品模块</th>
<th>缩写</th>
<th>核心功能</th>
<th>典型用途</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>Knowledge Advisor</strong></td>
<td>KWA</td>
<td>公式、规则、检查、反应特征、列表、循环</td>
<td>知识建模核心，定义设计逻辑</td>
</tr>
<tr>
<td><strong>Knowledge Expert</strong></td>
<td>KWE</td>
<td>专家规则库、Rule Base、Rule Set</td>
<td>大规模企业标准编码、合规检查</td>
</tr>
<tr>
<td><strong>Product Knowledge Template</strong></td>
<td>PKT</td>
<td>Power Copy、User Feature、Publication</td>
<td>可重用设计模板创建与发布</td>
</tr>
<tr>
<td><strong>Generative Knowledge</strong></td>
<td>KG</td>
<td>知识模式脚本、批处理生成</td>
<td>自动化生成复杂几何/特征序列</td>
</tr>
<tr>
<td><strong>Business Knowledge</strong></td>
<td>BK</td>
<td>企业知识定义、字典、报告</td>
<td>行业标准知识库构建</td>
</tr>
<tr>
<td><strong>Knowledge Integration</strong></td>
<td>KIN</td>
<td>外部数据集成（Excel/数据库）</td>
<td>与 PLM/ERP 系统数据桥接</td>
</tr>
</tbody>
</table>
<hr />
<h2 id="_3">二、核心功能详解</h2>
<h3 id="21-parameters">2.1 参数（Parameters）</h3>
<p>参数是知识工程的<strong>基础数据单元</strong>，所有设计变量都通过参数表达。</p>
<p><strong>参数类型：</strong></p>
<table>
<thead>
<tr>
<th>类型</th>
<th>说明</th>
<th>示例</th>
</tr>
</thead>
<tbody>
<tr>
<td>长度（Length）</td>
<td>线性尺寸</td>
<td><code>孔径 = 10mm</code></td>
</tr>
<tr>
<td>角度（Angle）</td>
<td>角度值</td>
<td><code>倒角角度 = 45deg</code></td>
</tr>
<tr>
<td>实数（Real）</td>
<td>无量纲数值</td>
<td><code>安全系数 = 2.5</code></td>
</tr>
<tr>
<td>整数（Integer）</td>
<td>整数值</td>
<td><code>螺栓数量 = 4</code></td>
</tr>
<tr>
<td>字符串（String）</td>
<td>文本</td>
<td><code>材料 = "AL6061-T6"</code></td>
</tr>
<tr>
<td>布尔（Boolean）</td>
<td>是/否</td>
<td><code>是否需要倒角 = true</code></td>
</tr>
<tr>
<td>特征（Feature）</td>
<td>引用几何特征</td>
<td><code>参考面 = PartBody\Plane.1</code></td>
</tr>
<tr>
<td>列表（List）</td>
<td>有序集合</td>
<td><code>尺寸列表 = [10, 20, 30]</code></td>
</tr>
</tbody>
</table>
<p><strong>关键特性：</strong><br />
- 支持多值参数（Multiple Values）— 限制可选范围<br />
- 支持增量模式（Incremental）— 自动按步长递增<br />
- 可在规格树中直接拖放关联<br />
- 支持 Rename — 通过语义化命名提升可读性</p>
<h3 id="22-formulas">2.2 公式（Formulas）</h3>
<p>公式定义参数间的<strong>数学关系和逻辑依赖</strong>。</p>
<p><strong>语法结构：</strong></p>
<pre><code>目标参数 = 表达式
</code></pre>
<p><strong>常用函数库：</strong></p>
<table>
<thead>
<tr>
<th>类别</th>
<th>函数</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td>数学</td>
<td><code>sin()</code>, <code>cos()</code>, <code>tan()</code>, <code>sqrt()</code>, <code>abs()</code>, <code>pow()</code></td>
<td>标准三角和数学运算</td>
</tr>
<tr>
<td>数学</td>
<td><code>round()</code>, <code>ceil()</code>, <code>floor()</code>, <code>int()</code></td>
<td>取整函数</td>
</tr>
<tr>
<td>统计</td>
<td><code>min()</code>, <code>max()</code>, <code>mean()</code></td>
<td>极值和均值</td>
</tr>
<tr>
<td>字符串</td>
<td><code>length()</code>, <code>concat()</code>, <code>substring()</code></td>
<td>文本操作</td>
</tr>
<tr>
<td>测量</td>
<td><code>distance()</code>, <code>angle()</code>, <code>area()</code>, <code>mass()</code></td>
<td>几何测量</td>
</tr>
<tr>
<td>零件</td>
<td><code>GetMinimumThickness()</code>, <code>GetVolume()</code></td>
<td>物理属性</td>
</tr>
<tr>
<td>曲线</td>
<td><code>Curvature()</code>, <code>Length()</code></td>
<td>曲线分析</td>
</tr>
</tbody>
</table>
<p><strong>示例：</strong></p>
<pre><code>// 标准件选型
螺栓直径 = if (载荷 &gt; 5000N) { 12mm } else { 8mm }

// 壁厚计算（基于压力容器公式）
壁厚 = (设计压力 × 内径) / (2 × 许用应力 × 焊缝系数 - 设计压力) + 腐蚀裕量

// 模态参数化
齿轮模数 = if (传递功率 &lt;= 5kW) { 2 } else if (传递功率 &lt;= 20kW) { 3 } else { 4 }

// 关联设计
安装孔间距 = 外壳宽度 - 2 × 边距
</code></pre>
<h3 id="23-rules">2.3 规则（Rules）</h3>
<p>规则是<strong>条件-动作</strong>对，当条件满足时自动执行操作，是最强大的知识表达方式。</p>
<p><strong>类型：</strong></p>
<table>
<thead>
<tr>
<th>规则类型</th>
<th>触发方式</th>
<th>典型场景</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>Rule</strong>（手动规则）</td>
<td>手动触发或通过宏调用</td>
<td>复杂逻辑分派</td>
</tr>
<tr>
<td><strong>Reaction</strong>（反应规则）</td>
<td>参数变化时自动触发</td>
<td>联动更新、自动修正</td>
</tr>
<tr>
<td><strong>On-Change Reaction</strong></td>
<td>特定参数变化触发</td>
<td>精确监听</td>
</tr>
<tr>
<td><strong>Time-driven Rule</strong></td>
<td>定时触发</td>
<td>周期性检查（少用）</td>
</tr>
</tbody>
</table>
<p><strong>Rule 语法结构：</strong></p>
<pre><code>Rule 规则名称
  Let 变量声明
  If 条件表达式
    动作语句
  Else
    备选动作
  EndIf
</code></pre>
<p><strong>实战示例：</strong></p>
<pre><code>// 示例1：标准件自动选型规则
Rule 螺栓选型规则
  Let boltGrade (String)
  If 载荷等级 == &quot;轻载&quot;
    boltGrade = &quot;4.8&quot;
    螺栓直径 = 6mm
  Else if 载荷等级 == &quot;中载&quot;
    boltGrade = &quot;8.8&quot;
    螺栓直径 = 10mm
  Else
    boltGrade = &quot;12.9&quot;
    螺栓直径 = 14mm
  EndIf
  螺栓规格 = concat(&quot;M&quot;, int(螺栓直径/mm), &quot;-&quot;, boltGrade)
EndRule

// 示例2：钣金工艺检查反应
Reaction 钣金厚度检查
  Let minBendRadius (Length)
  minBendRadius = 板厚 * 2
  If 弯曲半径 &lt; minBendRadius
    Message(&quot;警告：弯曲半径小于最小值（板厚×2）&quot;)
    弯曲半径 = minBendRadius
  EndIf
EndReaction

// 示例3：孔位阵列自适应
Rule 孔位自适应布局
  Let spacing (Length)
  spacing = (边长 - 2 * 边距) / (孔数 - 1)
  If spacing &lt; 最小间距
    Message(&quot;孔间距过小，建议减少孔数或增大边长&quot;)
  EndIf
EndRule
</code></pre>
<h3 id="24-checks">2.4 检查（Checks）</h3>
<p>检查用于<strong>验证设计合规性</strong>，不修改参数，仅报告状态。</p>
<p><strong>语法：</strong></p>
<pre><code>Check 检查名称
  Condition = 布尔表达式
  Message(&quot;不满足时的提示信息&quot;)
  Severity = 0  // 0=信息, 1=警告, 2=错误
EndCheck
</code></pre>
<p><strong>应用场景：</strong></p>
<pre><code>// 最小壁厚检查
Check 最小壁厚检查
  Condition = 最小壁厚 &gt;= 2mm
  Message(&quot;壁厚不足2mm，可能影响结构强度&quot;)
  Severity = 2
EndCheck

// 材料匹配检查
Check 材料合规检查
  Condition = 材料密度 in [2.7, 7.85]  // 仅允许铝/钢
  Message(&quot;材料不在允许范围内&quot;)
  Severity = 1
EndCheck

// 装配干涉检查
Check 装配间隙检查
  Condition = 最小间隙 &gt; 0.5mm
  Message(&quot;装配间隙不足，存在干涉风险&quot;)
  Severity = 2
EndCheck
</code></pre>
<p><strong>检查报告类型：</strong><br />
- ✅ <strong>OK</strong>（绿色）— 满足条件<br />
- ⚠️ <strong>Warning</strong>（黄色）— 警告级别<br />
- ❌ <strong>Error</strong>（红色）— 严重违规</p>
<h3 id="25-design-tables">2.5 设计表（Design Tables）</h3>
<p>设计表将<strong>Excel/文本表格</strong>与 CATIA 参数关联，实现系列化产品设计。</p>
<p><strong>工作机制：</strong></p>
<pre><code>Excel 表格 → 设计表关联 → 参数驱动 → 自动生成变体
</code></pre>
<p><strong>表格结构示例（法兰系列）：</strong></p>
<table>
<thead>
<tr>
<th>法兰型号</th>
<th>DN</th>
<th>外径</th>
<th>螺栓孔数</th>
<th>螺栓孔径</th>
<th>厚度</th>
<th>密封面直径</th>
</tr>
</thead>
<tbody>
<tr>
<td>DN50</td>
<td>50</td>
<td>165</td>
<td>4</td>
<td>18</td>
<td>20</td>
<td>102</td>
</tr>
<tr>
<td>DN80</td>
<td>80</td>
<td>200</td>
<td>8</td>
<td>18</td>
<td>24</td>
<td>133</td>
</tr>
<tr>
<td>DN100</td>
<td>100</td>
<td>235</td>
<td>8</td>
<td>22</td>
<td>26</td>
<td>158</td>
</tr>
<tr>
<td>DN150</td>
<td>150</td>
<td>300</td>
<td>12</td>
<td>22</td>
<td>30</td>
<td>208</td>
</tr>
</tbody>
</table>
<p><strong>使用方式：</strong><br />
1. 在 Excel 中创建参数表（首行为参数名）<br />
2. 通过 <code>Insert &gt; Knowledge &gt; Design Table</code> 导入<br />
3. 选择要关联的参数<br />
4. 通过切换行实现设计变体</p>
<p><strong>高级用法：</strong><br />
- 嵌套设计表：主表+子表联动<br />
- 条件过滤：<code>Filter = (DN &gt;= 50) and (DN &lt;= 200)</code><br />
- 宏驱动切换：通过 VBA/CAA 自动遍历所有配置</p>
<h3 id="26-catalogs">2.6 目录（Catalogs）</h3>
<p>目录是<strong>可重用设计元素的有组织集合</strong>，类似"超级零件库"。</p>
<p><strong>目录内容类型：</strong><br />
- 零件（Parts）<br />
- 装配体（Assemblies）<br />
- Power Copy<br />
- User Feature<br />
- 文档（Documents）<br />
- URL 链接</p>
<p><strong>目录层级结构：</strong></p>
<pre><code>Catalog: 企业标准件库
├── Chapter: 紧固件
│   ├── Family: 螺栓
│   │   ├── M6×20
│   │   ├── M8×30
│   │   └── M10×40
│   ├── Family: 螺母
│   └── Family: 垫圈
├── Chapter: 轴承
│   ├── Family: 深沟球轴承
│   └── Family: 圆锥滚子轴承
└── Chapter: 型材
    ├── Family: 角钢
    └── Family: 槽钢
</code></pre>
<p><strong>CATCatalogFile.catalog 文件格式：</strong></p>
<pre><code class="language-xml">&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot;?&gt;
&lt;Catalog&gt;
  &lt;Chapter Name=&quot;紧固件&quot;&gt;
    &lt;Family Name=&quot;螺栓&quot;&gt;
      &lt;Component Name=&quot;M6x20&quot; File=&quot;bolt_M6x20.CATPart&quot;/&gt;
      &lt;Component Name=&quot;M8x30&quot; File=&quot;bolt_M8x30.CATPart&quot;/&gt;
    &lt;/Family&gt;
  &lt;/Chapter&gt;
&lt;/Catalog&gt;
</code></pre>
<h3 id="27-power-copy">2.7 Power Copy</h3>
<p>Power Copy 是<strong>可参数化复用的特征组模板</strong>，将一组关联的设计特征打包为可重用模板。</p>
<p><strong>创建步骤：</strong><br />
1. 设计原始特征组（含完整参数和约束）<br />
2. <code>Insert &gt; Knowledge &gt; Power Copy</code> → 选择特征<br />
3. 定义输入参考（Input References）：哪些面/线/点需要用户指定<br />
4. 发布参数（Published Parameters）：哪些参数允许用户修改<br />
5. 保存到目录或文件</p>
<p><strong>应用流程：</strong></p>
<pre><code>选择 Power Copy → 指定输入参考（面/线/点）→ 修改参数 → 自动生成
</code></pre>
<p><strong>典型应用：</strong><br />
- 标准安装孔组（通孔+沉孔+倒角）<br />
- 散热齿阵列<br />
- 加强筋模板<br />
- 卡扣连接特征</p>
<h3 id="28-user-featureudf">2.8 User Feature（UDF）</h3>
<p>User Feature 是<strong>封装了内部逻辑的黑盒特征</strong>，对外只暴露接口参数。</p>
<p><strong>与 Power Copy 的区别：</strong></p>
<table>
<thead>
<tr>
<th>特性</th>
<th>Power Copy</th>
<th>User Feature</th>
</tr>
</thead>
<tbody>
<tr>
<td>内部可见性</td>
<td>完全可见</td>
<td>黑盒封装</td>
</tr>
<tr>
<td>参数暴露</td>
<td>全部可见</td>
<td>选择性暴露</td>
</tr>
<tr>
<td>编辑方式</td>
<td>编辑原始特征</td>
<td>仅修改暴露参数</td>
</tr>
<tr>
<td>保护性</td>
<td>无</td>
<td>内部逻辑受保护</td>
</tr>
<tr>
<td>适合场景</td>
<td>标准特征复制</td>
<td>专有设计封装</td>
</tr>
</tbody>
</table>
<p><strong>UDF 创建步骤：</strong><br />
1. 设计并测试原始特征<br />
2. <code>Insert &gt; Knowledge &gt; User Feature</code> → 选择特征<br />
3. 定义输入（Inputs）：参考几何<br />
4. 定义输出（Outputs）：结果几何<br />
5. 发布参数（Publications）：允许修改的参数<br />
6. 保存为 <code>.CATPart</code> 文件</p>
<h3 id="29-reactive-features">2.9 反应特征（Reactive Features）</h3>
<p>反应特征实现<strong>事件驱动的自动响应</strong>，当指定事件发生时自动执行。</p>
<p><strong>事件类型：</strong><br />
- <code>ValueChange</code> — 参数值变化<br />
- <code>AttributeChange</code> — 属性变化<br />
- <code>FeatureCreate</code> — 特征创建<br />
- <code>FeatureDelete</code> — 特征删除<br />
- <code>Open</code> — 文件打开<br />
- <code>Save</code> — 文件保存</p>
<p><strong>示例：</strong></p>
<pre><code>Reaction 自动标注更新
  Trigger = ValueChange(设计版本号)
  Action:
    图框版本 = 设计版本号
    日期 = currentDate()
    Message(&quot;设计版本已更新为: &quot; + 设计版本号)
EndReaction
</code></pre>
<h3 id="210-lists-loops">2.10 列表和循环（Lists &amp; Loops）</h3>
<p><strong>列表操作：</strong></p>
<pre><code>// 创建列表
Let myList (List)
myList = List(1, 2, 3, 4, 5)

// 列表操作
myList.Append(6)        // 添加元素
myList.Remove(2)         // 移除索引2的元素
myList.Size()            // 获取大小
myList.GetItem(3)        // 按索引获取

// 列表遍历
For i From 1 To myList.Size()
  // 操作 myList.GetItem(i)
EndFor
</code></pre>
<p><strong>循环结构：</strong></p>
<pre><code>// For 循环
For i From 1 To 孔数量
  Let angle (Angle)
  angle = 360deg / 孔数量 * (i - 1)
  // 创建孔特征在角度angle处
EndFor

// While 循环
Let count (Integer)
count = 0
While count &lt; 最大迭代次数
  count = count + 1
  // 迭代计算
EndWhile
</code></pre>
<h3 id="211-knowledge-pattern-generative-knowledge">2.11 知识模式脚本（Knowledge Pattern / Generative Knowledge）</h3>
<p>知识模式脚本用于<strong>批量自动生成重复性几何</strong>。</p>
<p><strong>脚本示例（散热齿阵列生成）：</strong></p>
<pre><code>KnowledgePattern 散热齿生成器
  // 输入参数
  Input: 基板长度 (Length), 齿高 (Length), 齿宽 (Length), 齿间距 (Length)

  // 计算
  Let 齿数 (Integer)
  齿数 = int(基板长度 / (齿宽 + 齿间距))

  // 循环生成
  For i From 0 To 齿数 - 1
    Let x (Length)
    x = i * (齿宽 + 齿间距)
    CreatePad(齿高, 齿宽, at=x)
  EndFor

  Output: 散热齿阵列几何体
EndKnowledgePattern
</code></pre>
<hr />
<h2 id="knowledge-expertkwe">三、Knowledge Expert（KWE）— 企业级规则引擎</h2>
<h3 id="31-rule-base-rule-set">3.1 Rule Base 与 Rule Set</h3>
<p>Knowledge Expert 提供结构化的<strong>规则库管理</strong>，是 KWA 的企业级升级版：</p>
<pre><code>Rule Base: 企业设计规范
├── Rule Set: 材料规范
│   ├── Rule: 材料选择规则
│   ├── Check: 材料合规检查
│   └── Rule Set: 有色金属子集    ← 可嵌套
│       └── Rule: 铝合金选型
├── Rule Set: 尺寸规范
│   └── Rule: 最小壁厚
└── Rule Set: 工艺规范
</code></pre>
<p><strong>关键概念：</strong><br />
- <strong>Rule Base</strong> = 顶层容器，一个 Part 可有多个 Rule Base<br />
- <strong>Rule Set</strong> = 逻辑分组（类似代码中的 package/module），可嵌套<br />
- <strong>Rule</strong> = 产生式规则（If-Then），<strong>可修改</strong>参数值<br />
- <strong>Check</strong> = 约束检查（布尔表达式 + 诊断消息），<strong>不能</strong>修改参数</p>
<h3 id="32">3.2 推理机制</h3>
<ol>
<li><strong>前向推理</strong>（默认）：从已知条件 → 推导结论 → 修改参数</li>
<li>KWE 中所有 Rule 都是前向推理</li>
<li>执行顺序由 Rule Set 内的位置或优先级决定</li>
<li><strong>无后向推理</strong>：KWE 不支持目标驱动的反向推理链</li>
<li>如需"已知目标求参数" → 用 KWA 的优化功能或外接求解器</li>
<li><strong>冲突消解策略</strong>：</li>
<li>优先级数值越高越优先（默认=0）</li>
<li>同优先级时后定义的覆盖先定义的</li>
<li><code>NoLoop</code> 关键字防止循环触发</li>
</ol>
<h3 id="33">3.3 三种求解模式</h3>
<table>
<thead>
<tr>
<th>模式</th>
<th>英文</th>
<th>执行逻辑</th>
<th>适用场景</th>
</tr>
</thead>
<tbody>
<tr>
<td>顺序</td>
<td>Sequential</td>
<td>从上到下逐条执行</td>
<td>规则间无冲突、执行顺序重要</td>
</tr>
<tr>
<td>冲突消解</td>
<td>Conflict Resolution</td>
<td>优先级高的覆盖低的</td>
<td>多条规则可能影响同一参数</td>
</tr>
<tr>
<td>传播</td>
<td>Propagation</td>
<td>变化沿依赖链传播</td>
<td>参数联动、级联更新</td>
</tr>
</tbody>
</table>
<h3 id="34-kwe">3.4 KWE 扩展语法</h3>
<pre><code>Rule 专家规则名
  Priority = 10
  NoLoop        // 防止循环触发

  Let 材料 (String)
  Let 应力 (Real)

  If PartBody\材料参数 == &quot;Q235&quot;
    if 屈服应力 &gt; 235MPa
      PartBody\安全系数 = 1.5
    endif
  EndIf
EndRule
</code></pre>
<h3 id="35-check">3.5 Check + 诊断报告</h3>
<pre><code>Check 壁厚检查
  Condition = PartBody\壁厚 &gt;= PartBody\最小壁厚
  Message(&quot;壁厚 &quot; + PartBody\壁厚 + &quot;mm 低于最小要求 &quot; + PartBody\最小壁厚 + &quot;mm&quot;)
  Severity = 2    // 0=信息(蓝), 1=警告(黄), 2=错误(红)
EndCheck
</code></pre>
<p><strong>诊断报告</strong>（Diagnostic Report）：<br />
- Tools &gt; Generate Report → 输出 HTML/TXT<br />
- 列出所有 Check 的通过/失败状态<br />
- 可嵌入企业审核流程<br />
- 失败的 Check 标红并附 Message 文本</p>
<pre><code>┌─────────────────────────────────────────┐
│  KWE 诊断报告                           │
├──────────────┬──────┬───────────────────┤
│ 规则名称     │ 状态 │ 消息              │
├──────────────┼──────┼───────────────────┤
│ 最小壁厚检查 │ ❌   │ 壁厚1.5mm &lt; 2mm   │
│ 材料合规检查 │ ✅   │ AL6061-T6 合规    │
│ 拔模角度检查 │ ⚠️   │ 建议≥3°拔模      │
│ 装配间隙检查 │ ✅   │ 间隙0.8mm &gt; 0.5mm │
└──────────────┴──────┴───────────────────┘
</code></pre>
<h3 id="36-kwe-vs-kwa">3.6 KWE vs KWA 分工</h3>
<table>
<thead>
<tr>
<th>维度</th>
<th>KWA</th>
<th>KWE</th>
</tr>
</thead>
<tbody>
<tr>
<td>定位</td>
<td>知识建模（参数+公式+规则）</td>
<td>知识执行与合规验证</td>
</tr>
<tr>
<td>规则规模</td>
<td>单条，零件级</td>
<td>Rule Base，企业级</td>
</tr>
<tr>
<td>合规检查</td>
<td>简单 Check</td>
<td>诊断报告 + 审核流程</td>
</tr>
<tr>
<td>用户角色</td>
<td>设计工程师</td>
<td>知识工程师/标准化部门</td>
</tr>
<tr>
<td>跨 Part 引用</td>
<td>需 Publication</td>
<td>需 Publication + Rule Base 链接</td>
</tr>
<tr>
<td>与 CAA 交互</td>
<td><code>CATICkeParmFactory</code></td>
<td><code>CATIKweRuleSet</code> / <code>CATIKweRuleBase</code></td>
</tr>
</tbody>
</table>
<h3 id="37">3.7 企业级最佳实践</h3>
<ol>
<li><strong>Rule Set 按标准分类</strong>：一个 Rule Set = 一个国标/企标</li>
<li><strong>Check 全覆盖</strong>：每个关键参数至少配一个 Check + Severity=2</li>
<li><strong>优先级分层</strong>：安全相关=100，功能相关=50，工艺偏好=10</li>
<li><strong>NoLoop 必加</strong>：修改参数的 Rule 一律加 NoLoop，否则 CATIA 崩溃</li>
<li><strong>诊断报告集成</strong>：设计评审前生成报告，替代人工检查清单</li>
<li><strong>版本管理</strong>：Rule Base 通过 Enoviav6/3DEXPERIENCE 管理版本</li>
</ol>
<hr />
<h2 id="caa-api">四、CAA API 知识工程接口</h2>
<p>通过 CAA（C++）API 可以编程访问知识工程功能：</p>
<h3 id="41">4.1 参数访问</h3>
<pre><code class="language-cpp">// 获取参数集
CATICkeParmFactory_var spParmFactory = spPart;

// 创建参数
CATICkeParm_var spDiameter = spParmFactory-&gt;CreateLength(&quot;螺栓直径&quot;, 10.0);

// 读取参数值
double value = spDiameter-&gt;Value()-&gt;AsReal();

// 设置参数值
CATCkeGlobals::SetParmValue(spDiameter, 12.0);

// 查找参数
CATICkeParm_var spParm;
CATCkeGlobalFunctions::GetParmDictionary()-&gt;FindParm(&quot;壁厚&quot;, spParm);
</code></pre>
<h3 id="42">4.2 公式操作</h3>
<pre><code class="language-cpp">// 创建公式
CATICkeFormula_var spFormula;
spParmFactory-&gt;CreateFormula(&quot;壁厚公式&quot;, &quot;壁厚&quot;, 
    &quot;(设计压力*内径)/(2*许用应力*焊缝系数-设计压力)+腐蚀裕量&quot;,
    CATListValCATICkeParmVar(参数列表));
</code></pre>
<h3 id="43">4.3 规则执行</h3>
<pre><code class="language-cpp">// 获取规则集
CATIKweRuleSet_var spRuleSet = spPart;
CATIKweRule_var spRule = spRuleSet-&gt;GetRule(&quot;螺栓选型规则&quot;);

// 执行规则
spRule-&gt;Execute();
</code></pre>
<h3 id="44">4.4 设计表操作</h3>
<pre><code class="language-cpp">// 关联设计表
CATIDesignTable_var spTable;
spTable = spPart-&gt;CreateDesignTable(&quot;法兰表&quot;, &quot;C:\\法兰系列.xls&quot;);
spTable-&gt;Associations()-&gt;AddAssociation(&quot;DN&quot;, &quot;公称直径&quot;);
</code></pre>
<hr />
<h2 id="_4">五、典型应用场景</h2>
<h3 id="1">场景1：系列化产品设计</h3>
<pre><code>参数表（Excel） → 设计表关联 → 公式计算 → 自动生成变体
</code></pre>
<p>应用：法兰系列、轴承选型、电机系列、型材截面</p>
<h3 id="2">场景2：设计标准合规检查</h3>
<pre><code>企业标准 → Rule Base → 检查规则 → 诊断报告 → 不合规预警
</code></pre>
<p>应用：航空适航检查、汽车安全标准、建筑规范</p>
<h3 id="3">场景3：智能装配</h3>
<pre><code>装配约束 + 知识规则 → 自动匹配 → 参数化装配 → 冲突检测
</code></pre>
<p>应用：管道自动布线、线束走线、设备布局</p>
<h3 id="4">场景4：工艺性审查</h3>
<pre><code>几何特征 → 工艺规则检查 → DFM 报告 → 修改建议
</code></pre>
<p>应用：钣金弯曲检查、铸造拔模角、机加工可达性</p>
<hr />
<h2 id="3dexperience">六、与 3DEXPERIENCE 平台的融合</h2>
<p>CATIA 3DEXPERIENCE 版本将知识工程进一步扩展：</p>
<table>
<thead>
<tr>
<th>功能</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>Engineering Template</strong></td>
<td>增强版 Power Copy，支持 3DEXPERIENCE 平台</td>
</tr>
<tr>
<td><strong>Knowledge Pattern</strong></td>
<td>可视化脚本，拖拽式定义生成逻辑</td>
</tr>
<tr>
<td><strong>ENOVIA 规则集成</strong></td>
<td>企业标准直接关联 PLM</td>
</tr>
<tr>
<td><strong>多学科联合仿真</strong></td>
<td>KBE 驱动 MODSIM（建模+仿真）闭环</td>
</tr>
</tbody>
</table>
<hr />
<h2 id="ai-catia">🔍 AI 融入 CATIA 知识工程的研究方向</h2>
<h3 id="1llm">方向1：LLM 辅助规则生成</h3>
<ul>
<li>用自然语言描述设计意图 → LLM 自动生成 KWA 规则代码</li>
<li>减少工程师学习曲线，降低 KBE 采用门槛</li>
</ul>
<h3 id="2_1">方向2：智能参数推荐</h3>
<ul>
<li>基于历史设计数据训练 ML 模型 → 自动推荐参数值</li>
<li>例如：给定载荷工况，自动推荐螺栓规格、壁厚、材料</li>
</ul>
<h3 id="3_1">方向3：设计合规性智能审查</h3>
<ul>
<li>将企业标准/AIAG/AS9100 等标准数字化 → LLM 解读 → 自动生成 Check 规则</li>
<li>传统做法需要人工逐条编码规则，AI 可以加速这个过程</li>
</ul>
<h3 id="4-kbe">方向4：生成式设计 + KBE</h3>
<ul>
<li>AI 生成拓扑优化方案 → KBE 编码设计约束 → 自动生成制造可行的 CAD 模型</li>
<li>CATIA 2026x 已推出 AI-Driven Generative Experiences</li>
</ul>
<h3 id="5">方向5：知识图谱驱动的智能设计</h3>
<ul>
<li>将企业设计知识构建为知识图谱 → 图检索 + LLM 推理 → 自动化设计决策</li>
</ul>
<hr />
<h2 id="_5">🔗 关联性</h2>
<ul>
<li><a href="../catia-automation.md">CATIA 自动化开发</a> — Knowledgeware 的编程接口</li>
<li><a href="../2026-05-21-catia-vba-batch-body-processing.md">CATIA VBA 批量处理</a> — VBA 宏开发实战</li>
<li><a href="struct-gen">结构智能选型系统</a> — KBE 在结构工程的应用</li>
</ul>
<h2 id="_6">💡 启发</h2>
<ul>
<li>KBE 的核心价值不是"自动化画图"，而是<strong>捕获设计意图和工程约束</strong></li>
<li>AI + KBE 的最佳切入点是<strong>规则生成</strong>（降低 KBE 编码门槛）和<strong>参数优化</strong>（超越人工经验）</li>
<li>CATIA 2026x 的 AI-Driven Generative Experiences 证明了这个方向的商业价值</li>
</ul>
<h2 id="_7">🔮 预判</h2>
<ul>
<li>3-5年内，LLM 将成为 KBE 规则编写的主要入口</li>
<li>AI 自动生成 Power Copy / UDF 模板将成为标配</li>
<li>基于知识图谱的智能设计系统将超越传统 Rule Base</li>
</ul>
<hr />
<h2 id="_8">参考资料</h2>
<ol>
<li>Dassault Systèmes — CATIA Knowledge Advisor (KWA) 官方文档</li>
<li>Dassault Systèmes — CATIA Knowledge Expert (KWE) 官方文档  </li>
<li>CATIA V5 Knowledgeware User Guide (catiadesign.org)</li>
<li>Dassault Systèmes — AI-Driven Generative Experiences (3ds.com)</li>
<li>CATIA CAA Encyclopedia — Knowledge Interfaces</li>
<li>3DEXPERIENCE CATIA R2026x — Visual Scripting &amp; Generative Design</li>
</ol>
<h2 id="_9">相关笔记</h2>
<ul>
<li><a href="catia_automation.md">catia_automation</a></li>
<li><a href="catia-automation.md">catia-automation</a></li>
<li><a href="explore/2026-05-22-CATIAKnowledgeware使用步骤指南.md">CATIAKnowledgeware使用步骤指南</a></li>
<li><a href="2026-05-21-catia-v6-custom-feature-development.md">catia-v6-custom-feature-development</a></li>
<li><a href="2026-05-25-CATIA-CAA-mkmk编译器Skill创建指南.md">CATIA-CAA-mkmk编译器Skill创建指南</a></li>
</ul>
</body>
</html>