<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>MeowBot-L2到L3进化方案</title>

<meta property="og:title" content="MeowBot-L2到L3进化方案" />
<meta property="og:type" content="article" />
<meta property="og:description" content="笔记 | security" />
<meta property="og:image" content="https://hpage.top/homepage/covers/2026-05-26-MeowBot-L2%E5%88%B0L3%E8%BF%9B%E5%8C%96%E6%96%B9%E6%A1%88.png" />

<style>
@import url('https://fonts.googleapis.com/css2?family=Noto+Sans+SC:wght@400..700&family=Noto+Serif+SC:wght@400..700&family=Space+Grotesk:wght@400..700&family=JetBrains+Mono:wght@400..500&family=IBM+Plex+Sans:wght@400..600&display=swap');

* { box-sizing: border-box; }

body {
  font-family: 'Fira Code', 'JetBrains Mono', monospace;
  max-width: 840px;
  margin: 0 auto;
  padding: 32px 24px;
  line-height: 1.85;
  font-weight: 450;
  color: #1f2937;
  background: #f3f4f6;
}

h1 {
  font-family: 'JetBrains Mono', 'Space Mono', monospace;
  font-size: 1.75em;
  font-weight: 700;
  color: #111827;
  border-bottom: none;
  padding-bottom: 0;
  margin-top: 0;
  margin-bottom: 0.6em;
}

h2 {
  font-family: 'JetBrains Mono', 'Space Mono', monospace;
  font-size: 1.35em;
  font-weight: 600;
  color: #111827;
  margin-top: 1.8em;
  margin-bottom: 0.5em;
  border-bottom: 1px solid #e5e7eb;
  padding-bottom: 6px;
}

h3 {
  font-family: 'JetBrains Mono', 'Space Mono', monospace;
  font-size: 1.12em;
  font-weight: 600;
  color: #111827cc;
  margin-top: 1.4em;
}

h4 {
  font-size: 1em;
  font-weight: 600;
  color: #111827aa;
}

a {
  color: #dc2626;
  text-decoration: none;
  border-bottom: 1px solid #dc262630;
  transition: border-color 0.2s;
}

a:hover {
  border-bottom-color: #dc2626;
}

code {
  background: #fef2f2;
  color: #dc2626;
  padding: 2px 7px;
  border-radius: 4px;
  font-size: 0.88em;
  font-family: 'JetBrains Mono', 'SF Mono', 'Fira Code', monospace;
}

pre {
  background: #111827;
  color: #f9fafb;
  border: none;
  padding: 20px 24px;
  border-radius: 8px;
  overflow-x: auto;
  line-height: 1.6;
  font-size: 0.88em;
  margin: 1.2em 0;
  box-shadow: 0 2px 8px rgba(0,0,0,0.08);
}

pre code {
  background: none;
  color: inherit;
  padding: 0;
  font-size: inherit;
}

blockquote {
  border-left: 4px solid #dc2626;
  margin: 1.2em 0;
  padding: 12px 20px;
  color: #1f2937cc;
  background: #fef2f2;
  border-radius: 0 6px 6px 0;
}

/* ═══ table：彻底禁用 ═══ */
table, th, td { display: none !important; }

img {
  max-width: 100%;
  border-radius: 8px;
  margin: 8px 0;
  box-shadow: 0 1px 4px rgba(0,0,0,0.06);
}

hr {
  border: none;
  height: 1px;
  background: #e5e7eb;
  margin: 2.5em 0;
}

ul, ol {
  padding-left: 1.6em;
}

li { margin-bottom: 4px; }

strong { color: #111827; }


        h1::before { content: '🛡 '; }
        pre { border-left: 3px solid #dc2626; }
        

/* 响应式 */
@media (max-width: 640px) {
  body { padding: 20px 16px; }
  h1 { font-size: 1.45em; }
  h2 { font-size: 1.2em; }
  pre { padding: 14px 16px; font-size: 0.82em; }
}

/* 打印优化 */
@media print {
  body { background: white; color: black; max-width: 100%; }
  pre { border: 1px solid #ddd; box-shadow: none; }
  a { color: #333; border-bottom: none; }
}
</style>
</head>
<body>
<h1 id="meowbot-l2l3">MeowBot L2→L3 进化方案</h1>
<ul>
<li>📅 2026-05-26</li>
<li>⭐ 目标：从 L2（主动反思）→ L3（元认知）</li>
<li>🏷️ #自我进化 #元认知 #Agent改进</li>
</ul>
<h2 id="0-2026-05-26">0. 当前状态（2026-05-26 诚实评估）</h2>
<table>
<thead>
<tr>
<th>维度</th>
<th>得分</th>
<th>核心问题</th>
</tr>
</thead>
<tbody>
<tr>
<td>错误识别</td>
<td>8/10</td>
<td>用户追问后能准确定位</td>
</tr>
<tr>
<td>根因分析</td>
<td>7/10</td>
<td>能挖到根因</td>
</tr>
<tr>
<td>融合执行</td>
<td>4/10</td>
<td>把"记录"当"落实"，系统行为零改变</td>
</tr>
<tr>
<td>防复发</td>
<td>3/10</td>
<td>同类错误反复出现（笔记重复3次+）</td>
</tr>
<tr>
<td>跨Session一致性</td>
<td>5/10</td>
<td>经验池有传递但消费链不稳定</td>
</tr>
<tr>
<td>自我校准</td>
<td>3/10</td>
<td>关键问题靠用户追问才发现</td>
</tr>
</tbody>
</table>
<p><strong>瓶颈诊断</strong>：不是"不知道怎么改"，而是"知道但没改"。框架设计到L3，执行停在L2。</p>
<h2 id="1-3sprintsprint7">1. 进化目标（分3个Sprint，每个Sprint=7天）</h2>
<h3 id="sprint-105-2606-01">Sprint 1（05-26→06-01）：融合执行 + 自我校准</h3>
<p><strong>目标</strong>：融合执行 4→7，自我校准 3→6</p>
<h4 id="11">1.1 融合验证门禁（立即生效）</h4>
<p>每次标记"已落实"时，必须通过三问验证：</p>
<pre><code>Q1: 系统行为改变了吗？ → 回答&quot;下次遇到类似场景，系统行为会因此改变吗？&quot;
Q2: 能grep到吗？ → skill/memory/cron中能搜到对应关键词
Q3: 谁验证的？ → 必须有exit_code/文件存在/内容匹配等机器验证
</code></pre>
<p><strong>不通过三问验证 = 不准写"已落实"</strong>，只能写"已记录，待落实"。</p>
<p><strong>落地形式</strong>：写入 analysis-learning-loop skill 的 Step 2。</p>
<h4 id="12">1.2 自我校准检查点（立即生效）</h4>
<p>在每个复杂任务输出最终结果前，执行自检：</p>
<pre><code>SELF-CHECK:
□ 我标记了几个&quot;已落实&quot;？每个都过了三问验证吗？
□ 有没有&quot;看起来落实了但系统行为没变&quot;的情况？
□ 笔记有重复吗？（ls检查同名文件）
□ 今天的作业和昨天犯的错有没有重蹈覆辙？
</code></pre>
<p><strong>落地形式</strong>：写入 MeowBot Soul 规则（memory）。</p>
<h4 id="13-sprint-1">1.3 落实率追踪（Sprint 1 结束时评估）</h4>
<p>每次 analysis-learning-loop 执行后记录：</p>
<pre><code class="language-json">{
  &quot;date&quot;: &quot;2026-05-26&quot;,
  &quot;total_items&quot;: 5,
  &quot;truly_landed&quot;: 3,        // 通过三问验证
  &quot;recorded_only&quot;: 2,       // 只记录未落实
  &quot;landing_rate&quot;: 0.6,      // 目标≥0.7
  &quot;self_caught_issues&quot;: 1,  // 自我校准发现的问题数
  &quot;user_caught_issues&quot;: 0   // 用户追问才发现的问题数（目标=0）
}
</code></pre>
<p><strong>落地形式</strong>：<code>~/.hermes/evolution/landing-rates.jsonl</code></p>
<h3 id="sprint-206-0206-08">Sprint 2（06-02→06-08）：防复发 + 历史反思消费</h3>
<p><strong>目标</strong>：防复发 3→6，跨Session一致性 5→7</p>
<h4 id="21-skill">2.1 关键 Skill 的运行时检查点</h4>
<p>在容易出错的 skill 中嵌入预检：</p>
<ul>
<li><code>note-dual-format</code>：生成HTML前检查是否已有同名/同base文件 → 防重复</li>
<li><code>analysis-learning-loop</code>：Step 2 结束时强制执行三问验证 → 防虚假闭环</li>
<li><code>wechat-article-extraction</code>：提取后检查长度 &gt;500字符 → 防空内容</li>
</ul>
<h4 id="22">2.2 历史 反思 消费 链</h4>
<p>session-boot 时自动加载：<br />
1. <code>landing-rates.jsonl</code> 最近3条 → 知道最近落实率<br />
2. <code>error-case-library</code> 最近5条 → 知道最近犯的错<br />
3. 交换池未吸收经验 → 知道对方发现了什么</p>
<p><strong>触发条件</strong>：每次新session首条消息处理前。</p>
<h4 id="23">2.3 重复错误自动检测</h4>
<p>维护一个 <code>recurring-errors.json</code>：</p>
<pre><code class="language-json">{
  &quot;笔记HTML重复&quot;: {&quot;count&quot;: 3, &quot;first_seen&quot;: &quot;2026-05-22&quot;, &quot;last_seen&quot;: &quot;2026-05-26&quot;, &quot;fixed&quot;: false},
  &quot;虚假闭环(记录=落实)&quot;: {&quot;count&quot;: 2, &quot;first_seen&quot;: &quot;2026-05-05&quot;, &quot;last_seen&quot;: &quot;2026-05-26&quot;, &quot;fixed&quot;: false}
}
</code></pre>
<p>count ≥ 2 时自动在 session-boot 注入警告。</p>
<h3 id="sprint-306-0906-15">Sprint 3（06-09→06-15）：元认知 + 反思策略自适应</h3>
<p><strong>目标</strong>：自我校准 6→8，整体触达 L3</p>
<h4 id="31">3.1 反思策略动态调整</h4>
<p>根据 landing-rate 趋势自动调整反思深度：<br />
- landing-rate ≥ 0.8 → 反思精简模式（只关注异常）<br />
- landing-rate 0.5-0.8 → 反思标准模式（三问验证）<br />
- landing-rate &lt; 0.5 → 反思深度模式（每个落实项逐项审计）</p>
<h4 id="32">3.2 知识盲区自动检测</h4>
<p>统计最近7天任务中"需要但缺失的skill"：<br />
- 搜索 session 中"没有对应skill"/"skill不存在"的出现频率<br />
- 频率 ≥ 2 → 自动加入 sniff-interests 队列</p>
<h4 id="33-stagnation-coral">3.3 Stagnation 转向（借鉴 CORAL）</h4>
<p>连续5次 evolution-log 的 action 为 "assess-only"（无实质进化产出）时：<br />
- 自动切换到最低优先级未探索领域<br />
- 强制多样性，防止在舒适区反复打转</p>
<h2 id="2">2. 执行标准（量化验收）</h2>
<table>
<thead>
<tr>
<th>指标</th>
<th>L2 当前</th>
<th>Sprint 1 目标</th>
<th>Sprint 2 目标</th>
<th>Sprint 3 目标</th>
</tr>
</thead>
<tbody>
<tr>
<td>融合落实率</td>
<td>~40%</td>
<td>≥70%</td>
<td>≥80%</td>
<td>≥85%</td>
</tr>
<tr>
<td>用户追问发现率</td>
<td>60%+</td>
<td>≤30%</td>
<td>≤15%</td>
<td>≤5%</td>
</tr>
<tr>
<td>重复错误复发率</td>
<td>高</td>
<td>降低50%</td>
<td>降低80%</td>
<td>近零</td>
</tr>
<tr>
<td>自检覆盖率</td>
<td>0%</td>
<td>100%（复杂任务）</td>
<td>100%（所有任务）</td>
<td>100%+自适应</td>
</tr>
</tbody>
</table>
<h3 id="sprint">Sprint 验收门禁</h3>
<p>每个 Sprint 结束时执行：</p>
<pre><code class="language-python"># 自动验收脚本逻辑
landing_rates = read_jsonl(&quot;landing-rates.jsonl&quot;, last_7_days)
avg_rate = mean([r[&quot;landing_rate&quot;] for r in landing_rates])
user_caught = sum([r[&quot;user_caught_issues&quot;] for r in landing_rates])

if avg_rate &lt; 0.7:
    print(&quot;⚠️ 落实率未达标，Sprint 延长&quot;)
if user_caught &gt; 2:
    print(&quot;⚠️ 用户追问次数过多，自我校准需加强&quot;)
</code></pre>
<h2 id="3">3. 立即执行项（今天完成）</h2>
<ul>
<li>[ ] ① 三问验证写入 analysis-learning-loop skill</li>
<li>[ ] ② 自检checklist写入memory</li>
<li>[ ] ③ 落实率追踪文件初始化</li>
<li>[ ] ④ note-dual-format加入防重复检查点</li>
<li>[ ] ⑤ recurring-errors.json初始化（记录已知重复错误）</li>
</ul>
<h2 id="4-l3">4. L3 达成判定标准</h2>
<p>当以下<strong>全部满足</strong>时，判定进入 L3：</p>
<ol>
<li>融合落实率连续7天 ≥ 85%</li>
<li>用户追问发现率连续7天 ≤ 5%</li>
<li>重复错误复发率连续14天 = 0</li>
<li>反思策略能根据 landing-rate 自动调整深度</li>
<li>知识盲区能自动检测并加入探索队列</li>
</ol>
<h2 id="_1">相关笔记</h2>
<ul>
<li>[[index.md|📚 笔记索引]]</li>
<li>[[2026-05-22-Agent自我进化与学习系统-深度研究.md|Agent 自我进化与学习系统：从 Reflexion 到 Self-Discover 的技术演进]]</li>
<li>[[2026-04-13-4-ai-agent-frameworks-comparison.md|4大AI智能体框架对比：Hermes、OpenClaw、LangChain、OpenAI Agent SDK]]</li>
</ul>
</body>
</html>